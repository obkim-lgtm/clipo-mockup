# 세특 간편 생성기 (간편 세특) 작업 컨텍스트

마지막 작업: 2026-05-28
다음 PC / 새 세션에서 이어받을 때 이 문서 먼저 읽기.

> ⚠️ **배포 일정 메모 (2026-05-28)**
> 지금까지 만든 mockup은 **이상안(ideal)** 이다. 실제 개발은 일정상 밀려 **10월 배포 예정**.
> 6/25 배포에는 **간편 세특 제외**. 10월 작업 시 "기존 개발 화면 유지 + 최소 요소만 추가" 방향.
> → 실제 작업 시점에 **개발된 화면 캡처 받아서 mockup과 diff** → 최소 추가분만 반영하는 방식으로 진행.
> → PRD는 그때 작성 (지금 쓰면 스코프 유동적이라 이중 작업). 이 문서가 맥락 보존 1차 소스.

## 개요
교과 단위로 학생 세특 초안을 일괄 생성하는 흐름. 좌측 네비 `zap` 아이콘 활성.
배포: https://obkim-lgtm.github.io/clipo-mockup/output/ 아래

## 전체 흐름

```
[목록] ── + 세특 추가 모달 ──→ 카드 추가 (학생 등록 전 상태)
   │
   ├── 카드 클릭 (학생 0명) ──→ [참여 학생 등록]
   │                            저장 → ?registered=&count= → 목록 복귀 후 진행률 표시
   │
   ├── 카드 클릭 (학생 N명) ──→ [학생 자료 입력] (3-step)
   │        1단계 학생 자료 입력 → 2단계 AI 자료 분석 → 3단계 기록 확인 및 수정
   │
   └── ⋮ 메뉴: 이름 변경 / 참여 학생 관리 / 삭제
```

## 개발 화면 1:1 재현본 (2026-05-28~)

> 실제 개발 화면 캡처를 받아 **이상안과 별개로** 1:1 그대로 그리는 트랙 시작. 이상안 `_v1_260526` 3종은 보존.
> 파비콘은 이 트랙 전용 **`favicon_gan_orange.svg`** (주황 #f97316 배경 + 흰 '간').

### `output/quick_seteuk_dev_v1_260528.html` — [키워드 및 활동 입력] 단계 (개발본)
- 5-step 스텝바(아이콘+라벨, 번호 없음): 기본 정보 설정 ▶ 그룹 구성 ▶ **키워드 및 활동 입력**(active) ▶ AI 기록 생성 ▶ 기록 편집
- 타이틀 `세특 간편 생성기 : 테스트` / 헤더 `대표교사`·D-1652·크레딧 750
- 좌: `학생 선택` 카드(반 select + 학생목록 라디오, 태그 `입력 내역:O`/`수행 수준:중`) + `↑ 키워드 일괄 업로드`
- 우: `키워드 사전 보기`(토글 on) 4개 카테고리(수와연산/변화와관계/도형과측정/자료와가능성) 칩 그리드 + 학생 입력 textarea + 수행수준 상/중/하 + `초기화`/`저장 후 다음 학생`
- 하단 `이전 단계`/`다음 단계`, 푸터 회사소개·이용약관·개인정보처리방침·고객센터
- ※ 이상안과 구조 다름(이상안=3step, 개발본=5step). 나머지 단계 캡처 받으면 같은 파일에 추가 예정

### `output/quick_seteuk_dev_edit_v1_260528.html` — [기록 편집] 단계 (개발본, 5step 중 마지막)
- 상단: 반 select + `전체 AI 기록 가져오기`(outline)·`저장`(blue)·다운로드(green icon)
- 테이블 7컬럼: 학번/이름/수행 수준/키워드 및 활동/AI Assistant가 생성한 기록/(가져오기 버튼)/선생님 직접 작성 — 모두 중앙 정렬
- AI 기록 셀: 회색 박스 + 글자수(N자 NBytes) + ‹ › + 복사 + `다시 생성`(blue, wand). 선생님 직접 작성 = textarea(placeholder "작성된 기록이 없습니다.") + 실시간 글자수
- 더미: 10101 김두나(209자)/10102 김세나(162자), 둘 다 수행수준 중

### 피그마 1:1 리스킨 (2026-05-28) — 개발 캡처 → 피그마 디자인 기준으로 전환
> 사용자 지시 "피그마랑 똑같이". Figma `u64nCDkeJkJoN1aSkSViLG` node **5334-12815**(키워드), **5335-13600**(기록 편집).
> 3개 선택 모두 "피그마 그대로": ①키워드 카테고리 ②기록편집 구성 ③헤더값.
- **헤더값 전부 교체**: 학교 유료 / D-180 / AI 크레딧 200(sparkle 아이콘) / 최대표 선생님(대표교사). 스텝 구분자 ▶→chevron-right
- **키워드 화면**: 카테고리를 교과식(수와연산 등)→**창체식**(자신과의 관계/타인과의 관계/사회 및 공동체와의 관계/자연과의 관계)로 교체. 선택칩 5개(감정조절·자기 관리·타인배려·타인존중·사회참여). 좌측 학생목록 40명(앞 2명만 입력배지). 키워드 일괄 업로드=우측 카드 하단 풀폭 버튼으로 이동. textarea는 placeholder만(빈값). 수행수준=연결형 segmented(상/중/하)
  - ⚠️ **카테고리 교과 정합성 이슈 보고함**: 창체식 키워드가 세특(교과)에 맞는지 사용자 확인 대기 — 현재는 피그마 placeholder 그대로 반영
- **기록 편집 화면**: 실제 2명 데이터 → **피그마 5상태 쇼케이스**(모두 10101 김하나). ①완료(302자+다시생성) ②생성전(생성된 기록 없음+기록 생성) ③생성중(스켈레톤+생성중 스피너) ④자료없음(안내문+0Byte+기록 생성) ⑤에러(빨강 네트워크 오류 문구+되돌리기+기록 생성). 상단 타이틀 우측 `일괄 기록 생성` 버튼, 액션줄 `09/08 15:38 저장됨`+`전체 기록 옮기기`+`저장`+다운로드. 반 select=이동수업 로봇공학 전문공학 A반

### 개별 파일 업로드 + AI 분석 추가 (2026-05-28, 키워드 화면)
- 키워드 사전 ↔ "학생 입력 내용" 사이에 `활동 자료 업로드` 섹션 신설
- 파일 1개만, PDF·PNG·JPG, 20MB 이하 (input accept + JS 검증, 초과/타입오류 alert)
- 드롭존(클릭/드래그) → 파일 행(아이콘+이름+크기+x) → `AI 분석`(sparkles) 버튼 + `크레딧 1` pill + 안내문
- 분석 클릭 → 1.5s 스피너(분석 중) → `AI 분석 결과` 블록 노출(AI 추출 배지 + 안내 + **수정 가능한 textarea**) + 버튼 `다시 분석`으로 전환
- 결과란은 "학생 입력 내용"과 **별도 입력란**(교사 수정 가능). 좌측 액센트 띠 없이 배경+배지로 AI 출처 구분
- 타이틀 폰트 20px(사용자 지시), 키워드 일괄 업로드는 좌측 학생 패널 하단 연파랑 버튼

### 키워드 사전 → 토글 아코디언 + 위치 이동 (2026-05-28)
- 기존 세특 이상안(`quick_seteuk_input_v1_260526`)의 `.kwbox` chevron 아코디언 패턴 적용. 타이틀 "키워드 사전"(← "키워드 사전 보기" Switch 제거), **기본 접힘**, 클릭 시 `.expanded` 토글(chevron 90° 회전)
- 위치: 우측 카드를 `display:flex; flex-direction:column`(`.kw-card`) + `order`로 재배치 → 업로드/AI → **학생 입력 내용** → **키워드 사전(아코디언)** → 수행 수준 → 액션. (DOM 소스 순서는 그대로, order로만 시각 배치 — 대용량 카테고리 마크업 안전 유지)
- 칩 bg #fff+border(아코디언 배경 #fafbfc 위 대비), 14px 유지

### 키워드 화면 디테일 정리 (2026-05-28)
- 학생 식별: 하늘색 배너 제거 → 섹션 제목을 `{학번 이름} 키워드 및 활동 입력`(예 "10101 김하나 키워드 및 활동 입력")으로. 학생 선택 시 `#curStuName`+`#inputLabel` 자동 갱신
- 학생 목록 태그 3개 한 줄: `자료 O/입력 O/수준 중`(있음=파랑/없음=회색 `.off`), nowrap. 명단 학번순 자연 이름(10101 김하나·10102 김두나·10103 박서연·이준호·최유진·정민서·강하준·윤지아·임도윤)
- 키워드 칩 클릭 → 위 `#inputContent`(학생 입력 내용)에 콤마 이어붙임/해제 시 제거. 초기 선택칩은 `initInput()`으로 입력란에 미리 반영
- 키워드 사전 안내문 "…선택할 수 있습니다."
- AI 분석 버튼: 파일 카드 **우측 인라인** `[AI 분석 · 크레딧 1]`(분석 후 `다시 분석 · 크레딧 1`), 파일 행 아래 한 줄 안내
- 직접 입력란 라벨 = `키워드 및 활동 직접 입력`(정적, 학생명 제거 — 제목에 이미 있음). 업로드 섹션 설명에 크레딧 안내 통합("…분석해 드려요. 분석 1회당 크레딧 1개가 사용돼요."), 버튼 옆 중복 안내 제거
- **명단 태그 동적화**: 활성 학생의 `자료`/`입력` 태그가 편집 상태와 실시간 동기화(`updateActiveTags()`). 자료=파일 업로드 여부(fileBox 표시), 입력=`#inputContent` 텍스트 유무. 파일/키워드 **독립(자율)** — 파일만/키워드만/둘다 모두 가능. 예시: 10101=키워드만(자료X·입력O), 10102=파일만(`김두나_활동보고서.pdf`, 자료O·입력X)
- **학생별 상태 저장/복원 추가**: `studentData{학번:{file,content,keywords,level}}`. 학생 선택 시 직전 학생 `saveActive()`→선택 학생 `loadStudent()`(파일박스·입력·칩·수준 복원). 목록 태그는 데이터에서 `renderTags()`로 생성 — 자료·입력 둘 다 없으면 태그 없음(시작 전), 하나라도 있으면 3개. → **목록 태그 = 그 학생 실제 편집 상태**로 항상 일치(이전 mismatch 해결)

### 4단계(AI 기록 생성) 신규 + 5단계 기록 다듬기 이식 (2026-05-28)
- **`output/quick_seteuk_dev_ai_v1_260528.html`** — Step 4 (Figma node **5359-12950** 기준). 반별 아코디언(1학년 1~4반) + 학생 테이블(체크박스+전체선택 / 학번·이름·수행수준·키워드및활동내역·상태). 상태=생성 완료(파랑)/생성 실패(빨강)/초안 없음(회색·비활성). 아코디언 헤더 우측 `N명 선택`/`선택 없음` 뱃지 동적. 2반 기본 펼침·2명 선택. 하단 `이전 단계`/`AI 기록 생성`(→편집)
- **5단계 기록 다듬기 이식**(`quick_seteuk_dev_edit_v1_260528.html`): 이상안 `quick_seteuk_input_v1_260526` 의 다듬기 기능을 `rf-` 접두사로 self-contained 포팅. 표 첫 컬럼 체크박스(AI 기록 보유 행만 활성)+전체선택 / 상시 블루 액션바(0명→안내+[기록 다듬기], N명→선택 해제+[N명 기록 다듬기]) / **다듬기 모달**(정보 하늘색 박스·좌측 띠 없음 + 수정 방향[선택]·300자·퀵 프롬프트 5종 + 다듬을 학생 picker + [N명 다듬기·N크레딧]) + 토스트. eligible=AI 기록 보유 학생(쇼케이스라 10101 김하나 1명). 더미라 다듬기 실행은 토스트만. ※ 행별 [다듬기] 진입점은 생략(체크박스+바로 대체, single 모드 함수는 남겨둠), Figma 행의 [다시 생성]은 유지

### 4·5단계 추가 정리 (2026-05-28)
- 스텝바 사이즈 축소(개발 인스펙터 기준): 높이 48→40px, padding 0 18, radius 8, 폰트 15, 아이콘 16 — 3개 화면 공통
- 4단계 열 순서: 수행 수준을 키워드 및 활동 내역 **뒤(상태 왼쪽)**로 이동(3단계 입력 순서와 일치). 키워드 및 활동은 **1열 유지**(3단계가 단일 입력란이라 분리 안 함)
- 5단계: ①우측 상단 `일괄 기록 생성` 버튼 제거 ②다듬기 안내문 "…한꺼번에 **AI가 생성한 기록**을 다듬을 수 있어요"로 변경 + 파랑(#365eef)·15px·볼드 강조 ③반 select 볼드(700) ④수행 수준 열을 키워드 뒤로 이동(4단계와 동일) ⑤스텝바 아래 액션줄+다듬기바+표를 **흰색 카드 한 덩어리**(`.edit-card`)로 묶음 ⑥AI 기록/선생님 직접 작성 **동일 너비 28%/28%**(`table-layout:fixed`+전열 % 지정), `AI 분석 결과`→`AI 활동 분석 결과`

### 스텝바 4단계 포함 연결 (2026-05-28)
- 4개 화면 스텝바 상호 연결: 키워드(dev_v1) ↔ AI 기록 생성(dev_ai) ↔ 기록 편집(dev_edit). 키워드 `다음 단계`→AI, AI `이전 단계`→키워드·`AI 기록 생성`→편집

### 스텝바 화면 간 이동 연결 (2026-05-28)
- `.step-pill.nav` 클래스 + `onclick="location.href=..."`로 연결. `키워드 및 활동 입력` ↔ `기록 편집` 양방향 이동됨
- 나머지 3단계(기본 정보 설정/그룹 구성/AI 기록 생성)는 화면 미제작 → 클릭 비활성(cursor:default). 캡처 받으면 파일 만들고 같은 방식으로 연결

### AI 작업 중 차단 정책 적용 (2026-05-28, 창체 원칙 이식)
> 창체에서 확정한 "AI 작업 중(반영 중)엔 수정·실행 차단" 원칙을 dev 3종에 적용. 줄리/제이콥 협의 결과.
- **통일 툴팁/토스트**: `"AI 작업 중에는 수정할 수 없어요. 완료 후 다시 시도해 주세요"` (모든 파일 공통)
- **공통 구현**: 각 파일에 `BUSY_TIP` / `_busy` / `setBusy(on)` + `body.is-busy` CSS dim + **캡처 단계 클릭 인터셉터**(작업 중 모든 클릭 차단 후 토스트)
- **차단 단위 = 페이지(작업) 전체**: 간편세특은 학생 1명씩 편집 흐름이라 창체식 "학생 단위 vs 일괄" 구분 없음. AI 작업이 도는 동안 그 페이지 전체 잠금이 자연스러움
- **`dev_v1`(키워드·활동 입력)**: `runAnalyze`(AI 분석 1.5s) 동안 → 학생 전환·키워드 칩·입력 textarea(disabled)·수행 수준·파일 삭제·드롭존·초기화·저장·일괄 업로드·단계 이동 전부 잠금. 분석 버튼은 기존 스피너 유지
- **`dev_ai_v1`(AI 기록 생성)**: `[AI 기록 생성]`=`startGenerate()`로 변경(기존 location.href 직행 제거). 선택 0명이면 토스트, 선택 시 → 체크박스·아코디언·이전 단계 잠금 + 버튼 "생성 중..." 스피너 → 1.3s 후 편집 페이지로 이동
- **`dev_edit_v1`(기록 편집)**: 개별 `[기록 생성]`=`genOne()`(버튼 "생성중" 스피너+전체 잠금 1.5s), 다듬기 `rfRun()`=모달 닫고 "다듬고 있어요…"+잠금 1.6s 후 완료 토스트. 잠금 대상: 저장·전체 옮기기·다운로드·가져오기·기록생성·다듬기·체크박스·반 select (`wasDis`로 원래 비활성 상태 보존)
- 검증: 3파일 모두 작업 중 잠금·클릭 차단·완료 후 자동 복구·콘솔 에러 없음 확인. 커밋 `a4fbe0f`

## 파일 (이상안 — 보존)

### 1. `output/quick_seteuk_list_v1_260526.html` — 목록
- 학년도별 3컬럼 카드 그리드
- 카드: `[과목] ⋮ / 제목 / 수정일 / 진행률 X/Y명 OR "학생 등록 전"`
- `card.dataset.classMode` = `'academic'|'mobile'` (반 편성 기준)
- **`+ 세특 추가`** 모달 (※ "교과 추가" 아님 — 라벨 변경됨):
  - 기본 정보: 학년도(2026/2027) · 반 편성 기준 · 이름
  - 교과 및 성취기준: 교육과정 / 과목 / 성취기준 N개 (최대 10, 직접 입력 토글)
- ⋮ 메뉴 3개: 이름 변경(모달) / 참여 학생 관리(등록 페이지) / 삭제(Figma red dialog)
- **온보딩 팝업** — 첫 진입 시 "이제 활동지·보고서도 올릴 수 있어요" (3단계 흐름 안내 + 다시 보지 않기). localStorage 키 `clipo_quick_seteuk_onboarding_seen`. `?ob=1` 강제 표시. (입력 페이지에 있던 걸 목록으로 이동함)
- 지난 학년도 카드 진행률 회색 톤다운
- MOCKUP 토글: 데이터 있음/없음 (드래그)
- `?registered=&count=&subject=` 받으면 매칭 카드 갱신 또는 새 카드 prepend

### 2. `output/quick_seteuk_register_v1_260526.html` — 참여 학생 등록
- 헤더: `참여 학생 등록 : {이름}` + back-link (스텝바 없음 — 의도적)
- 2단 레이아웃 (좌: 등록 방법 / 우: 콘텐츠), 방식 3개:
  - 학생 목록에서 선택 / 학생 명단 업로드(엑셀) / 학생 명단+학생 특성 업로드(엑셀)
- 학적기준반(`?classMode=academic`) vs 이동수업반(`?classMode=mobile`)
  - 학적: 학교 학생 트리 체크박스 / 이동: `+ 반 만들기` 모달 → 반 카드 아코디언 → 반별 배정
- MOCKUP 토글: 학적반/이동수업반 (드래그)
- 저장 → `quick_seteuk_list?registered=&count=&subject=` redirect

### 3. `output/quick_seteuk_input_v1_260526.html` — 학생 자료 입력 (★ 핵심, 가장 많이 작업)
3-step 헤더 (번호만, 아이콘 없음): `1 학생 자료 입력 / 2 AI 자료 분석 / 3 기록 확인 및 수정`

**Step 1 — 학생 자료 입력**
- 좌: 반 dropdown + 학생 목록(태그: 자료 등록 / 특성 입력 / 수준) + **일괄 자료 등록 2개**(PDF 일괄 / 학생 특성 일괄)
- 우: 학생 활동 자료 업로드(PDF/JPG/PNG · 1개 · 20MB) + **학생 특성** textarea + 키워드 사전(토글 접힘) + 수행 수준(상/중/하/자동)
- 검증: 자료 OR 학생 특성 중 하나 필수
- **마지막 학생 저장 시 녹색 완료 카드** (우측 입력 영역 하단, ▼화살표가 하단 [다음 단계] 가리킴)

**Step 2 — AI 자료 분석** (Figma 5126:27316 리스킨 적용)
- 좌 사이드바: "학생 명단" 타이틀 + 반 + "학생 목록 (Nm)" + 목록 (Step 1과 동일 패턴으로 통일)
- 우: 학생 정보 panel(이름·학번 + 상태 배지 + 메타 + [분석 시작]/[재분석]) + 자료 변경 시 주황 stale 배너
- 분석 완료 시: "AI 자료 분석 내용" panel(타이틀 + **sub-text 설명** + 파일 chips + 항목 카드) + 활동 내용 panel + 보충 내용 dashed 버튼
- 항목 카드: AI 추천/발견한 역량 pill 배지 + 본문 + 추천 근거 박스 + 자료 출처 chip
- **분석 전원 완료 시 녹색 완료 카드** (다음 단계 안내)

**Step 3 — 기록 확인 및 수정**
- 표: ☐체크박스 / 학번 / 이름 / AI 생성 기록 / 가져오기 / 선생님 작성 기록 / 글자수 — 모두 중앙 정렬
- 상단 액션바: [전체 AI 기록 생성] / [전체 옮기기] / [엑셀]
- **[전체 AI 기록 생성] 모달** (full-width): 학생 picker + 분량 + AI 고급 설정 + 재생성 요청사항
- **기록 다듬기 (★ 후속 배포 검토용 mockup — 2026-05-28 추가)**:
  - 표 첫 컬럼 체크박스 + 헤더 select-all
  - **상시 노출 블루 액션바** (AI 기록 ≥1개일 때): 0명 → 안내문 + [기록 다듬기] / N명 → [선택 해제] + [N명 기록 다듬기]
  - 행 개별 [다듬기] 버튼
  - **[기록 다듬기] 모달** (full-width): 수정 방향(선택) + quick prompt chips 5종 + 학생 picker(이름만) + [N명 다듬기·N크레딧]
  - 분량/고급설정 **없음** (자유입력으로 표현, 충돌 방지) · 비워두면 이전 생성 조건 그대로 재생성
- 하단 sticky: 진행률 + [이전 단계] + [저장] (다음 단계 없음 — 마지막 단계)
- MOCKUP 토글: 빈 화면 / 데이터 있음 (8학생 다양 케이스 A~F + D')

### 4. `output/favicon_gan.svg`
- 파란 배경(#416bff) + 흰 "간"

## 확정 디자인 결정 + 근거 (★ 휘발 방지 — 10월에 "왜?" 안 헤매도록)

| 결정 | 내용 | 근거 |
|------|------|------|
| **세특 추가** (← 교과 추가) | 목록 메인 버튼/모달 라벨 | "마이 교과 선택" 메뉴와 혼동. "교과 수업 추가"는 길고 어색. 서비스명과 일치 |
| **참여 학생 등록** (← 학생 등록) | 페이지 타이틀 | "등록"이 학교 학적등록과 충돌. 창체와 톤 통일 |
| **다시 생성 → 다듬기로 통합** | 모든 재생성 진입점 "다듬기" | 같은 입력 단순 재시도는 lottery·크레딧 낭비. 재생성 욕구=방향성 있음 → 명시화가 품질↑ |
| **다듬기 모달서 분량·고급설정 제거** | 수정 방향 자유입력만 | 자유입력("300자로 줄여")과 구조화 옵션(분량 풍부)이 **충돌**. 비워두면 이전 조건 유지 안내 |
| **출처 chips → 보류** | Step 3 AI 셀 출처 표시 | 행마다 메타 깔면 편집 워크스페이스 위계 깨짐. 정공법=행 클릭→drawer "이 기록의 출처". 코드엔 `s4SourceChipsHtml` 남겨둠(현재 비활성 가능) |
| **완료 카드 하단 배치** | Step1·2 녹색 카드 | "하단 버튼 누르세요" 안내인데 카드가 상단이면 시선 역행. ▼화살표로 버튼 가리킴 |
| **완료 카드 우측 영역 폭** | margin-left:320px | 사이드바까지 가로지르면 목록 길 때 안 보임 |
| **온보딩 입력→목록 이동** | 팝업 위치 | 기능 진입 전 목록에서 안내가 자연스러움 |
| **Step 2 Figma 5126:27316 리스킨** | 창체·간편세특 동일 적용 | 줄리 디자인 갱신. 두 제품 통일 |
| **테이블 중앙 정렬** | 모든 표 헤더·셀 | 메모리 `feedback_table_center_alignment` |
| **다듬기 모달 안내** 노랑→하늘색 | info 톤 | 경고 아님. "AI 생성 기록만 교체, 선생님 작성 기록 영향 없음" 명시 |

## 후속 분류 (Phase)

**이미 mockup 완성 (이상안):**
- 목록 / 참여 학생 등록 / 3-step 입력 전체 / 온보딩 / Step2 리스킨

**후속 배포 검토용으로 추가만 함 (개발 미확정):**
- **기록 다듬기** (Step 3) — 가장 마지막에 붙인 것. 10월 스코프에 들어갈지 미정

**보류/백로그:**
- Step 3 출처 표시 → drawer 정공법으로 추후
- 이동수업반 + 엑셀 업로드 세부

## 루카스 피드백 반영 (2026-05-28, 4건)
1. 교과 추가 → 세특 추가 ✅
2. 마지막 학생 저장 후 다음 단계 안내 (녹색 완료 카드) ✅
3. Step 2 "AI 자료 분석 내용" sub-text 설명 추가 ✅
4. AI 생성 기록 출처 표시 → chips 넣어봤으나 **옥빈님·Claude 모두 제거 의견** (drawer 정공법 백로그)

## 창체와의 관계
- `dong_register_v4` 구조 카피해서 등록 페이지 시작
- Step 2 동일 Figma 리스킨 (`creative_activity_input_v1_260507.html`)
- 출처 chips도 창체 Step 3에 동일 이식 (`s3SourceChipsHtml`, 동일하게 보류 의견)
- "다른 양식은 사용할 수 없어요." / 섹션 번호 제거 — 양쪽 동일

## 배포
- repo: `obkim-lgtm/clipo-mockup` (`master`) — GitHub Pages 자동
- 마지막 커밋: `a4fbe0f` (2026-05-28, dev 3종 AI 작업 중 차단 원칙 적용)
- 허브: `obkim-lgtm/hub` `main` — CLIPO 목업 섹션, **"6/25 배포 제외" 표시함**

## 환경 메모
- 로컬 경로: `<드라이브>:\내 드라이브\Claude\clipo_mockup\` (Windows=F:, 일부=G:)
- 미리보기: `preview_start('clipo-mockup')` 포트 3457, doc root는 `clipo_mockup/`이라 URL에 `/output/` 붙여야 함
- 한글 경로 Bash 못 잡을 때 PowerShell 우회
