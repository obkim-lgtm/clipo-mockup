# CLIPO PRD 작성 가이드

두 가지 예시 PRD(H-2605-00 평가 홈 개선, H-2604-00 평가 설계, C-2604-01 개별채점 개선)를 기반으로 정리한 작성 지침.

---

## 1. 파일 ID 규칙

```
C-YYMM-NN  제목
```

- `C` : CLIPO (HIAI는 `H`)
- `YYMM` : 작성 연월 (예: 2604 = 2026년 4월)
- `NN` : 순번 (00, 01, 02…)
- 제목은 기능명 + 핵심 동작 한 줄 (예: 수행평가 개별 채점 개선)

---

## 2. 문서 구조

```
## 1. 도입 배경 및 목적
## 2. 화면 구성 개요          ← 신규 화면이면 포함, 기존 화면 수정이면 생략 가능
## 3. 핵심 정책               ← 개발자가 판단해야 할 분기·조건·정책만
## 4. 주요 범위 (Scope)       ← ✅ 포함 / ❌ 제외
## 5. 유저 플로우             ← 코드블록 트리
## 6. 후속 과업               ← 있을 때만
## 목업
```

---

## 3. 섹션별 작성 원칙

### 1. 도입 배경 및 목적
- **왜 지금인가** + **무엇이 달라지는가** 2가지만
- 기획 의도(사용자 가치) → 개발 목표 순서로
- 후속 과업과 연결되는 경우 마지막 줄에 명시

> 예시
> - AI 채점 결과를 교사가 검토하고 확정하는 과정에서, 채점요소별 피드백이 별도 영역에 분리되어 있어 활용이 낮고 편집 경험이 불편했다.
> - 채점기준별 근거·피드백을 해당 요소 카드 내 인라인으로 통합하여 **채점과 피드백 작성을 한 흐름으로 처리**

### 2. 화면 구성 개요
- 탭 구조, 레이아웃 분할, 진입 경로가 있을 때만 작성
- 표 형태로 간결하게

### 3. 핵심 정책
**포함할 것**
- 상태 분기 조건 (어떤 상태에서 어떻게 동작하는가)
- 데이터 처리 규칙 (저장·삭제·덮어쓰기 정책)
- 단계 진입 조건 / 이탈 처리
- 정렬·노출 기준 (기본값 포함)

**포함하지 말 것**
- 색상·폰트·간격 등 UI 스타일 (목업으로 전달)
- 이미 반영된 기존 정책 반복
- "~할 수 있다" 식 기능 나열 → 조건과 결과로 바꿔 쓸 것

**변경 PRD는 Before → After 표로**

| 항목 | 기존 | 변경 |
|------|------|------|
| 과제물 탭 | 파일 목록 탭 UI | 드롭다운으로 교체 |

### 4. 주요 범위 (Scope)
- ✅ 포함: 이번 배포에 반드시 들어가는 것
- ❌ 제외: 논의됐지만 뺀 것 + 이유 한 줄 (이유 없으면 생략)

> 제외 이유 예시: `반(학급) 단위 진행 현황 분리 (복잡도 이슈로 전체 집계만 노출)`

### 5. 유저 플로우
- 코드블록 트리 형식 (들여쓰기로 분기 표현)
- 화면 이름은 대괄호 `[화면명]`
- 분기점은 `└─`, `├─`

```
[시작 화면]
  └─ 액션
        ↓
[다음 화면]
  ├─ 분기 A → 결과 A
  └─ 분기 B → 결과 B
```

### 6. 후속 과업
- 이 PRD가 선행 조건이 되는 다음 과업만 기재
- 표 형태: 과업명 | 내용

### 목업
- GitHub Pages 배포 URL 링크 1줄
- 로컬 URL(localhost) 사용 금지

---

## 4. 서술 수준 — PRD와 피그마 기획서의 분담 (2026-08-09 확정)

**PRD의 목적은 과업 범위 산정이다.** 화면 배치·문구·컴포넌트 상세는 **별도 피그마 기획서**에서 확정하므로 PRD에 옮겨 적지 않는다.

| PRD에 쓴다 | 피그마 기획서에 쓴다 |
|---|---|
| 무엇을 바꾸는가 (변경 지점 한 줄) | 화면 배치·컴포넌트·상태 |
| 분기 조건·데이터 처리·권한 정의 | 실제 노출 문구 |
| 범위 / 비범위와 그 사유 | 색·배지·간격 |

**규칙**
- 변경 항목은 `# | 무엇을 바꾸는가` **2열**로 쓴다. 개발 영향(FE/BE) 같은 열은 넣지 않는다 — 구현 판단은 개발이 한다.
- **문장은 개조식**(명사형 종결)으로. `A는 B다` 대신 `A = B`, `~를 추가함` 대신 `~ 추가`.
- **이미 구현된 것은 항목에서 뺀다.** 개발 화면 캡처로 먼저 확인할 것 — 캡처에 이미 있는 기능을 "정비"로 적으면 범위가 부풀려진다.
- **정책이 정해지지 않은 것도 뺀다.** 목업에 그려져 있어도 정책 정의가 없으면 범위가 아니다.
- 열려 있는 질문은 `개발 확인 필요`가 아니라 **`논의 필요`**로 적는다.
- 목업 링크에는 **"범위 참조용, 확정안 아님"**을 명시한다. 명세로 읽히면 문구까지 확정된 것으로 오해된다.
- 목업 해시(`#leave1` 등)를 적을 때는 **실제 파일에서 확인**하고 적는다. 존재하지 않는 앵커를 적어둔 사례가 있었다.

## 5. 분량 기준

| 유형 | 적정 분량 |
|------|-----------|
| 신규 기능 | 핵심 정책 3~5개 섹션, A4 2장 이내 |
| 기존 기능 개선 | Before/After 표 중심, A4 1장 이내 |
| 후속 연계 과업 | 배경 1문단 + 변경 표 + Scope만 |

---

## 6. 체크리스트

작성 후 아래를 확인한다.

- [ ] 개발자가 이 문서만 보고 API 구조를 설계할 수 있는가?
- [ ] UI 스타일 설명이 아닌 **조건·정책·데이터 처리** 중심인가?
- [ ] ❌ 제외 항목에 이유가 있는가?
- [ ] 목업 링크가 GitHub Pages URL인가? **해시가 실제 파일에 존재하는가?**
- [ ] 후속 과업이 있다면 명시했는가?
- [ ] **이미 구현된 것 / 정책 미정인 것이 범위에 섞여 있지 않은가?** (개발 화면 캡처로 대조)
- [ ] **문구·화면 상세가 PRD에 들어와 있지 않은가?** (§4 분담표)
- [ ] 절 번호가 겹치지 않는가? (정책 토글 `§n`과 본문 `n장`을 섞어 쓰면 참조가 헷갈린다)
