# 홈 리뉴얼 — 이어받기 (2026-09-25 밤)

> **🟢 새 세션 시작점(09-26 기준) — 홈은 v2로 확정**
> - **현행 = §9 'v2 확정'**이다. §6~§8(09-25~26 오전)은 v1에 이르기까지의 기록이다. 카드 안 할 일 · 할 일 순 정렬 · 평가 × 반 카드 표시 · 3열 타일 · 알약 등은 **v2에서 모두 대체**됐다.
> - 목업: `output/home_renewal_v2_260926.html`(원본, 까망이 4케이스 hr2*)과 `output/quick_start_v1_260925.html` `#home`(흐름 페르소나 김하나로 이식). **홈을 고치면 두 파일을 같이** 고친다. `home_renewal_v1_260925.html`은 비교용으로만 남긴다.
> - 시각 · 구조 규칙의 원본은 `../design-system.md` '홈·카드 시각 규칙'이다(v2 기준으로 새로 씀).
> - 다음 후보는 §9 끝 '남은 것'이다.
> - **배포(09-26 마감)**: GitHub `obkim-lgtm/clipo-mockup` master `25904c7`. 공개 주소 https://obkim-lgtm.github.io/clipo-mockup/output/home_renewal_v2_260926.html (`#home` · `#home-empty` · `#home-resume` · `#home-empty-resume`), 흐름 목업 https://obkim-lgtm.github.io/clipo-mockup/output/quick_start_v1_260925.html#home . 허브 카드는 아직 안 넣었다(목업 카드는 올립에게 먼저 묻기).
> - **마지막 결정(09-26)**: 임시 채점 줄 = `화산 지형 탐구 · 임시 채점 · 29일 남음 · [수업에 옮기기 ›]`. '가져오기 전'(뜻 안 보임) · '29일 뒤 지워져요'(김)는 기각.
> - **올립 답을 기다리는 질문**: 흐름 목업 `quick_start_v1_260925.html` 안의 `내 수업에 가져오기` 7곳을 홈 버튼 이름 `수업에 옮기기`에 맞출지(홈에서 누른 말 = 도착한 화면의 말). 답을 받기 전엔 고치지 않는다.
> - **커밋에서 뺀 것**: `output/eval_design_quick_v1_260925.html`(평가 만들기 통합, 다른 세션 작업 중) · `service_overview.md`(09-21 개정분, 다른 세션). 홈 작업과 무관하니 건드리지 않는다.

> 작성 Olivia. '활동지로 바로 채점' 목업 작업 중에 "당장 개발이 어려우니 이 기회에 홈도 리뉴얼하자"로 커졌다. 목적이 달라서 새 세션으로 분리한다.
> 흐름 목업(`output/quick_start_v1_260925.html`)의 홈은 **개발 홈 + 새 입구 카드 1장**으로 되돌려 두었다. 리뉴얼 초안은 `output/home_renewal_draft_260925.html`에 그대로 보존(이 파일의 `#home`만 보면 된다. 나머지 화면은 흐름 목업 사본).

## 1. 왜 홈을 다시 짜나 — 진단편 근거
- 문제 정의 덱: `../../clipo_problem_definition/` (`context.md` · `클리포 문제정의_260925.pptx`). 숫자 출처 = `../../clipo_traffic_analysis/context.md` §3.999b.
- **P1 진입**: 인증 교사 60%가 30일 안에 채점을 안 함. 재방문 이탈의 80%는 수업 만들기 이전. 설계까지 하고 멈춘 교사(재방문 539명)는 81%가 교사 일괄 업로드를 골랐고 58%는 다시 와서도 안 올림 → **올리는 단계가 벽**.
- **P2 정착**: 실반 첫 채점자의 38%가 점수를 저장하지 않음(원인은 인터뷰 필요).
- **P3 발견**: 학생 제출 사용 교사 8%, 결과 공개 과제 5%, 기능 문의 38%가 '어떻게·어디서'. 안 쓰이는 기능은 전부 '평가 설계의 선택지'.
- **홈 온보딩 배너 투어 시작률 30% → 12%** — '들어올 때 알리기'는 효과가 없음. 안내는 행동하는 자리에서.
- 올립이 추가로 풀고 싶은 것(09-25): **업데이트된 기능을 사용자가 모름** · **연수(무료, 끝까지 참여 시 AI 크레딧 200개 쿠폰)를 모름** · 블로그(채점 예시·가이드 영상) 노출을 높이고 싶음.

## 2. 정해진 방향
- **할 일 중심.** 올립: "내가 뭐 하면 될지, 할 거 중심으로." '수업 목록 + 할 일 띠를 덧붙인 것'은 여전히 목록 중심이라 탈락.
- **수업 카드 = 그 수업의 '다음 할 일' 하나 + 버튼**(초안에 구현). 수업에 붙은 일은 수업 카드에서(올립: "수업 카드에 넣어서 수업 연계되는 게 더 낫다").
  - 다음 할 일 사다리: 시작 전(평가 만들기) → 답안 기다림(스캔 PDF 올리기 · 학생 제출로 받기) → 채점 → 확인 필요 → 결과 공개 전(결과 공개하기) → 세특. P1·P2·P3의 행동 지점이 카드 위에 그대로 뜬다.
  - 카드 위쪽은 개발 카드 그대로(이름·⋮ 메뉴), '수행평가 N개 · 과목'은 작은 한 줄.
- **수업이 없는 일**(새 입구의 임시 채점 · 채점기준 초안)은 수업 카드에 못 붙는다 → 자리 필요.
- 새 기능 알리기의 문법(다른 서비스 공통): ① 기능 자리의 `New` 배지 + 첫 사용 때 한 줄 ② 행동 순간의 제안 ③ 제품 안 '새 소식' 모음 ④ 제품 밖(메일·블로그 소식). 피하는 것 = 로그인 팝업·캐러셀·긴 투어. CLIPO는 학기 리듬이라 **"지난 학기 이후 달라진 것"**으로 묶는 게 맞다. 필요하면 실제 화면으로 벤치마크 조사해서 다시 바꾼다(확인된 것만 해결편에).
- 블로그 쪽 결정(`../../clipo_blog/context.md` §7): 홈 오른쪽 자료실 첫 카드 → 블로그 배우기 탭. 진짜 주목도는 기능 화면 안의 맥락 링크. 상단바 고정 영역엔 넣지 않는다.

## 3. 초안에서 해 본 것 · 올립 반응
| 해 본 것 | 반응 |
|---|---|
| 온보딩 배너 자리에 '새로 시작'(활동지로 바로 채점 + 과목별 채점 예시 링크, 임시 채점이면 이어보기로 바뀜) | **"좀 그렇다"** — 다시 생각할 것. 새 세션의 첫 과제 |
| 위쪽 '지금 할 일' 띠(확인 요청 띠를 넓힘) | 탈락 → 수업 카드로 |
| 점선 '2학기 수업부터 만들려면' 카드 | 탈락 — 헤더 '수업 만들기'와 중복, 새 입구보다 눈에 띔 |
| 오른쪽 레일: 지난 학기 이후 달라진 것([써 보기]·[확인했어요]) · 공지사항 · 연수 카드(이벤트 카드 자리) · 클리포 블로그 한 장 | 레일이 "너무 많다" → 학교 정보·자료실 뺐음. 유튜브는 갱신 안 하니 블로그 소개로. 달라진 것+공지사항 합치기는 미정 |
| 채점기준 화면에 블로그 영상 맥락 링크(R1 채점기준 잘 쓰는 법 2:26) | 흐름 목업에 남겨 둠 |

## 4. 새 세션에서 정할 것 — ✅ 09-25 전부 결정(§6)
1. **새 입구·임시 채점의 자리**(온보딩 배너 자리는 반려). 후보: 현재 학기 그리드 첫 칸 카드(흐름 목업에 쓰는 안) / 수업 카드 사다리의 '0번 칸' / 기타.
2. 수업 카드 사다리를 보여 줄 데이터 — 한 교사에게 수업 3~4개를 두고 단계별로? (가짜 데이터 늘리는 것이라 올립 확인 필요 — 메모리 `feedback_mockup_no_fake_data`)
3. 한 수업에 평가가 여럿일 때 어떤 할 일을 띄울지(제안: 마감 가까운 것 → 멈춘 지 오래된 것).
4. 오른쪽 레일 최종 구성(달라진 것 · 공지 합치기 여부, 연수 카드가 이벤트 자리를 써도 되는지 — 다른 팀 캠페인 일정 확인).
5. 체크리스트·개인정보 동의서 양식을 블로그 '학교 도입 자료'로 옮길지.
6. 기존 사용자 영향 — 홈은 모든 교사의 첫 화면이다. 덱의 "기존 화면은 그대로" 약속이 바뀌므로 해결편에 근거를 다시 쓴다.

## 6. 결정 (09-25 새 세션 · 올립 확정) — 목업 `output/home_renewal_v1_260925.html`
| # | 정할 것 | 결정 | 근거·메모 |
|---|---|---|---|
| 1 | 새 입구·임시 채점의 자리 | **A '빈 학기 = 시작 화면'**. 이번 학기 내 수업 0개면 그 학기 자리가 두 갈래 카드(활동지로 바로 채점 New / 수업 만들기, 같은 무게). 수업이 있으면 제목 줄 버튼 둘(`활동지로 바로 채점 New` + `수업 만들기`). 빈 학기일 땐 제목 줄 버튼 숨김(같은 행동 두 군데 금지). 임시 채점은 **학기 위 '수업 없이 채점한 것' 묶음**(어느 학기에도 속하지 않음, 있을 때만) | 입구 후보 ①첫 칸 ②제목 줄 ③레일을 보인 뒤 올립이 "원점에서 다시 잡아도 된다" → 뼈대 3안(A 빈 학기 / B 수업 한 줄+단계 막대 / C 1단+상단바 소식) 중 A. 반려된 배너와 다른 점 = 늘 얹히는 홍보 띠가 아니라 빈 학기의 빈 상태. 지난 학기는 접힌 채 시작, 접혔을 때만 헤더에 `할 일 N` |
| 2 | 사다리 데이터 | **2학기 수업 5개 추가**(역사 채점 전 · 도덕 점수 저장 전 · 미술 결과 공개 전 + 09-25 추가 요청으로 과학 설계 전(빈 문서 일러스트) · 국어 세특 전) → 2학기 한 화면에 6단계 전부, 최근 활동순 = 사다리 순서 + 기존 2학기 사회 답안 받기 전. 1학기 사회 세특 전 · 1학기 미술 설계 전(수행평가 0개로 바꿈). 학생은 늘리지 않음 | 라벨을 '~전'으로 통일: 설계 전 / 답안 받기 전 / 채점 전 / **점수 저장 전 / 결과 공개 전**(09-25 올립: '저장 전'만으론 무엇을 저장하는지 헷갈림 → 대상을 붙임) / 세특 전 (+ 공유받은 수업 '확인 요청'). 저장 전 문구 = "저장해야 세특에 쓸 수 있어요"(근거 `cs_assist/product_facts_clipo.md` 세특 대상 = 점수 저장 학생) |
| 3 | 평가가 여럿일 때 | **끝에 가까운 단계부터**(점수 저장 전 > 결과 공개 전 > 채점 전 > 답안 받기 전 > 설계 전), 같은 단계면 최근에 손댄 것. 카드 단계 줄 `평가 N개 더`. 올립 재확인(09-25): 한 수업 안의 여러 평가를 동시에 처리하지 않으니 하나씩 보이는 게 맞음 | 마감 기준은 안 씀 — 마감 데이터가 대부분 비어 있음(`clipo_problem_definition/context.md`). 코드 `pickNext()` |
| 4 | 오른쪽 레일 | **공지사항(운영 안내만) · 연수(개발 홈 이벤트 자리 공유 — 캠페인 있을 땐 캠페인) · 블로그 배우기 탭**. 학교 정보·자료실 없음. **'지난 학기 이후 달라진 것'은 본문 열**(이번 학기 수업 뒤·지난 학기 앞, 접기 기억, 다 확인하면 사라짐). 기능 공지는 공지 목록에 두지 않음(중복) | 처음엔 '소식' 카드로 합치기로 했다가 벤치마크로 바꿈(아래 §7). 블로그 카드 목적지 = `learn.html`(`../../clipo_blog/context.md` §7) |
| 5 | 체크리스트·동의서 | **쓰는 자리로 나눔**: 체크리스트 = 요금제 상단 배너(이미 제품에 있음) 그대로, 개인정보 동의서 양식 = 학교 설정 › 학생 등록 화면 링크. 블로그 '학교 도입 자료'엔 공유용 원본만 | 해당 화면 목업은 아직 없음 |
| 6 | 기존 사용자 영향 | 아래 표를 해결편에 그대로. 덱 문장 "기존 화면은 그대로, 홈 첫 칸 하나가…" → **"다니던 길은 그대로, 카드마다 다음 할 일이 보인다"** | 위험: 할 일 상자가 실제와 다르면 신뢰 상실 → 평가 상태 데이터(답안 수·채점·저장·공개)로 참/거짓이 갈리는 것만, 추정 금지 |

**기존 사용자 영향(해결편 7장 원본)** — 그대로인 것: 상단바·왼쪽 메뉴 · 카드 클릭 → 수업 홈 · ⋮ 메뉴(보관·수정·삭제) · 학기 묶음 · 공유받은 수업 묶음 · 정렬 · 보관 토글 · 수업 만들기 팝업.

| 자리 | 개발 홈 | 리뉴얼 | 근거 |
|---|---|---|---|
| 온보딩 배너 | 체험 시작하기(늘 보임) | 없음. 이번 학기 0개일 때만 두 갈래 시작 | 투어 시작률 30% → 12% |
| 제목 줄 | 정렬 · 보관 · 수업 만들기 | + 활동지로 바로 채점 | 재방문 이탈 80%가 수업 만들기 전 |
| 수업 카드 | 이름 · 수행평가 N개 · 태그 | + 다음 할 일 하나 · 버튼 | 설계 후 재방문해도 안 올림 58% · 저장 안 함 38% · 결과 공개 5% · 학생 제출 8% |
| 확인 요청 띠 | 홈 위 한 줄 | 공유받은 수업 카드의 할 일 | 같은 행동 두 군데 금지 |
| 지난 학기 | 펼침 | 접힘 + 할 일 N | 이번 학기 할 일 집중 |
| 새 기능 | 없음(공지 한 줄) | 본문 '지난 학기 이후 달라진 것' | Classroom 홈 모듈 배치 · 기능 문의 38% '어떻게·어디서' |
| 레일 | 공지 · 이벤트 · 자료실 · 학교 정보 | 공지 · 연수(이벤트 자리) · 블로그 배우기 | 레일 과밀 · 연수 인지 |
| 자료실 양식 | 홈 | 요금제 · 학교 설정 › 학생 | 쓰는 자리에서 |

**카드 구성 줄임(09-25 올립 "줄여줘")**: 라벨·설명·버튼이 같은 말을 세 번 하고, 색 상자 4색이 그리드를 대시보드처럼 만들어서 줄였다. 지금 카드는 **수업명 → 평가명 → `단계 · 라벨에 없는 사실` → 버튼**이다. 색 상자·아이콘·설명 문장은 없앴다. 둘째 줄에는 숫자나 결과만 쓴다(답안 28장 · 저장해야 세특에 쓸 수 있어요). 과목 태그는 수업명과 다를 때만 붙이고, `수행평가 N개` 줄은 뺐다. 임시 채점 단계 라벨은 `가져오기 전`이다. **위계(09-25 올립: 수업명이 덜 띔)**: 수업명 18px 굵게(20px은 과하다 — 09-25 올립, 개발 17px보다 한 단계만 키움). 수업명·평가명 길면 한 줄 말줄임 + title. **카드·달라진 것 버튼 = 흰 바탕 테두리(btn-secondary)**, 파란 채움은 수업 만들기(빈 학기면 두 갈래 카드 버튼)에만 — 09-25 올립 → 가는 구분선 → 평가명 16px 보통 굵기·보조색 → 단계 줄 14px(단계만 굵게) → 버튼.

**홈 디자인 입히기(09-25 올립)**: 블로그 스타일은 쓰지 않는다(CLIPO 제품 스타일과 어긋남). 제품 캡처(`CLIPO 홍보물/제품 화면/00_홈_260923.png`)에 이미 있는 표현만 가져왔다.
- NEW 태그: 바탕 #e9f2ff · 글자 #3151c4. 새 입구 버튼과 최근 공지에 붙였다.
- 이벤트 카드: 연보라 #d5bfff · 제목 #3c1c78 · `자세히 보기 ›` · 아래 점. 연수 ↔ 이용권 이벤트 2장 캐러셀이다.
- 자료실 줄: #f4f8ff 바탕 + 파란 아이콘. 블로그 배우기 한 줄이다.
- '달라진 것' 제목: 파란 선 아이콘 + 제목(연수센터 섹션 제목 모양). **본문은 흰 상자 하나 안의 목록 줄**(기능 아이콘 · 제목 · 한 줄 · `써 보기 ›` 링크 · 확인했어요) — 카드 두 장이 수업 카드처럼 보인다는 지적(09-25 올립)으로 모양을 갈랐다.
- **제품 일러스트(로켓 등)는 넣지 않는다**(올립: 로켓 별로). 대신 **빈 상태에만 새로 그린 인라인 SVG**(CLIPO 파랑 선 #416bff + 연한 면 #e9f2ff·#ccdfff): 빈 학기 두 갈래 카드(스캔 틀 속 활동지 · 칠판 앞 학생 셋) + 평가 없는 수업 카드(점선 빈 문서 + 연필, 오른쪽 아래). 할 일이 있는 수업 카드는 장식하지 않는다(복잡도).

**레일 구조 재정리(09-25 올립, §6-4 일부 번복)**
- 나누는 기준: **행동할 것은 본문, 알아 두면 좋은 것은 레일**이다. 레일 순서는 시간성 높은 것이 위다.
- ① **공지사항**: 연수는 공지가 맡는다. 연수 공지가 최소 일주일 전에 나가기 때문이다. 신청 기간 중인 연수는 맨 위에 고정하고(핀 아이콘) 날짜 대신 연수일까지 `D-N`을 보인다. 목업 = [9월] 국어과 실전 연수, 9/30 → D-5.
- ② **이벤트 = 캠페인 전용**: 연수 참여 카드는 뺐다(공지와 중복). 첫 장은 **연수 강사 안내 배너**다. 올립: "연수를 한다보다 강사를 위한 안내가 없다". 문구 사실 출처는 블로그 연수 탭 '연수 운영 지원'이다(강사용 PPT·과목별 채점 예시·인증 코드·강사와 참여자 전원 크레딧 200개 14일·연수일 3일 전 신청). 링크는 `training.html#instructor`. 둘째 장은 **채점기준 꿀팁 배너**다(09-25 올립: 이용권 기간 연장 대신). 제목은 '현직 교사가 들려준 / 채점기준 꿀팁'이고 링크는 '에듀테크 교사 연구회 공모전 결과 ›'로, 블로그 AI 채점 가이드 `guide/contest.html`로 간다. 옅은 파랑 바탕에 클립보드 체크리스트 + 전구 일러스트를 넣었다. **배너 디자인**: 연보라 바탕 + 연수센터 배너의 흰 원 무늬 + 혜택 칩 3개(강사용 PPT · 인증 코드 · 참여자 전원 크레딧 200개, 흰 반투명). 오른쪽 위에는 보라 선 일러스트(발표 화면 + 200 쿠폰)를 두었다. 설명 문장은 칩과 겹쳐서 뺐다.
- ③ **자료실**: **별도 덩어리로 둔다**(09-25 올립: 공지와 성격이 다르다). 줄은 두 개다.
  - `기능 배우기`: 제품 유튜브 줄의 모양(빨간 바탕 #fff3f4 + 재생 아이콘)을 그대로 쓰고, 목적지만 블로그 배우기 탭이다.
  - `개인정보 동의서 양식`: 개발 홈 문구 그대로다. **§6-5를 일부 번복**했다(09-25 올립: '기존처럼'). 동의서는 홈 자료실에 남긴다. 체크리스트는 그대로 요금제 배너다.
- 이벤트 배너 높이: 공지보다 눈에 띄지 않게 줄였다(09-25 올립). 제목 16px 두 줄 + 혜택 한 줄 링크(`강사용 PPT · 인증 코드 · 크레딧 200개 ›`)이고, 일러스트는 54px로 작게 했다. 높이는 약 105px로 이전의 절반이다. 색: 제품 연보라 #d5bfff는 너무 튀어서(09-25 올립) 옅은 보라 #f5f0ff + 얇은 테두리 #e6dbff로 바꿨다. 링크는 #6f34db다.
- **'지난 학기 이후 달라진 것'은 본문에서 펼친 채로 강조해 둔다**(09-25 올립: 기능 이야기라서). 할 일이 많아도 기본으로 접지 않는다. 교사가 접으면 그 상태를 기억하고, 다 확인하면 묶음째 사라지는 동작은 그대로다.
- 측정 후보: 고정된 연수 공지의 클릭률이다. 공지가 있는데도 연수를 모른다면 원인이 '안 읽음'일 수 있다.

**카드 그림 비교 · 시작 카드 크기(09-25 올립)**
- 단계별 그림 5개(답안 받기 전 = 겹친 답안지 + 올리기 / 채점 전 = 답안지 + 반짝이 / 점수 저장 전 = 점수표 + 체크 / 결과 공개 전 = 눈 / 세특 전 = 펼친 책 + 펜)를 **덜 강조해서** 그렸다. 크기 56px, 옅은 선 #7fa2ff다. 켜 보고 **채택하지 않았다**(올립: "없는 게 더 낫다" — 그림 6개가 버튼과 겨루고, 빈 상태 그림의 뜻이 흐려진다). 코드와 까망이 항목도 지웠다. **카드 그림은 빈 상태에만** 둔다(설계 전 빈 문서 · 빈 학기 두 갈래).
- 빈 학기 두 갈래 카드가 너무 커서 **가로형**으로 바꿨다(그림 88px 왼쪽 · 글 오른쪽 · 제목 18px). 높이는 약 400px에서 약 210px로 줄었다.

**'지난 학기 이후 달라진 것' 노출 조건(09-26 올립 확정)** — 학기가 아니라 **교사의 사용 이력**에 붙는다. 수업만 만들고 학기 말까지 아무것도 안 했을 수도 있기 때문이다.

| 조건 | 규칙 |
|---|---|
| 누구에게 | 지난 학기 이전부터 쓰던 교사만. 이번 학기 신규 가입자에게는 안 보인다('지난 학기 이후'가 성립 안 함). 새 교사의 기능 소개는 빈 학기 두 갈래와 행동하는 순간의 안내가 맡는다 |
| 무엇을 | 교사의 지난 학기 마지막 활동 이후 출시된 기능 중 **아직 안 써 본 것**(사용 이력으로 판단) |
| 언제 사라지나 | 확인했어요 · 써 보기 · 그 기능을 한 번 씀 · 학기 교체(새 묶음으로) |
| 어디에 | 이번 학기 뒤 자리는 그대로 두되 **위아래 선으로 학기 목록과 뗀다**. 제목은 17px로 학기(20px)보다 낮춘다 |

- 목업에 반영했다. 리뉴얼 홈 페르소나(최대표)는 공동 확인을 이미 써서(공유받은 수업·확인 요청) 그 소식이 빠지고 1개만 보인다. 흐름 목업(김하나)은 2개가 그대로다.
- **카드 개인화(평가명+버튼) 재검토 중**(09-26): 방향은 '수업 홈으로 보내기보다 원하는 일을 바로'로 유지한다. 방식은 LMS 벤치마크 조사 결과를 보고 정한다.

**카드 개인화 재검토 근거(09-26)**
- **LMS 벤치마크**: 할 일은 과목 카드 **밖** 합친 목록으로 둔다(Classroom Recently due = 최근 7일 · 검토 필요 최대 5개(원문 확인), Canvas To Do · Schoology Reminders · Moodle Timeline · Teams Ready to grade는 에이전트 확인). 카드는 입구로 두고 배지만 붙인다(Canvas · D2L). **'카드마다 다음 할 일 + 버튼'은 확인한 서비스에 없다.**
- **DB(26-1 유료 2,491명)**: 수업 수 중앙값 2 · 84%가 4개 이하 · 5개+ 16%. 같은 주 동시 활동 수업 1개 56% · 90%가 4개 이하. **반별 분할 62%.** 상세는 `../../../clipo_traffic_analysis/context.md` §3.999c.
- **결정(09-26 올립 '보완해주라')**: **A(카드마다 할 일)를 유지**하고 두 가지를 보완했다(두 파일 모두).
  - ① 정렬에 **`할 일 순`**을 추가하고 기본값으로 했다. 할 일 있는 카드가 먼저 오고, 순서는 점수 저장 전 → (확인 요청) → 결과 공개 전 → 채점 전 → 답안 받기 전 → 세특 전 → 설계 전이다. 할 일 없는 카드와 지난 학기 idle은 뒤로 간다. 같은 단계 안에서는 수업명순이다. 최근 활동순 · 수업명순은 그대로 둔다.
  - ② **반별 반복은 사실대로 둔다.** 여러 반을 한 번에 올리는 기능이 제품에 없어서 묶어 버튼 하나로 처리하지 않는다. 대신 할 일 순 정렬로 같은 평가 · 같은 단계의 반 카드가 나란히 붙는다.
  - ~~데모: 사회 2-1 · 2-2 · 2-3을 따로 만든 수업~~ → 아래 평가 × 반으로 대체했다.
- **평가 × 반(09-26 올립)**: 한 수업에 **여러 반을 넣는 경우가 더 흔하다**(올립 판단, DB 확인은 생략). 과제물 · 채점은 반 단위라 **할 일의 단위 = 평가 × 반**이다. 표시 방식 3안(①할 일 하나 + 반 표시 / ②평가 + 반별 요약 / ③반마다 한 줄) 중 **① 채택**. 복잡도는 세 가지로 묶는다.
  - 반 이름은 **반 2개+ 수업에만** 붙인다(`todo.ban`). 반이 하나인 수업은 그대로다.
  - 단계 줄은 한 줄로 쓴다: `단계 · 반 · 사실 1개`(예: `채점 전 · 2-3반 · 답안 26장`).
  - **'할 일 N개 더' → 숫자 없는 `나머지 할 일 ›`**(버튼 줄 오른쪽 → 수업 홈). 밀린 양을 들이밀지 않고, 규모는 수업 홈의 몫이다.
  - 우선순위 규칙(끝에 가까운 단계 → 최근)을 평가 × 반 조합 전체에 적용한다.
  - 데모: 2학기 사회 = 한 수업 안에 2-1 · 2-2 · 2-3반(인구 문제: 2-1 · 2-2 답안 받기 전, 2-3 채점 전 / 세계화: 설계 전) → 카드는 `채점 전 · 2-3반`. 역사는 반이 하나라 반 표시가 없다(대비).

## 7. 새 기능 알리기 벤치마크 (09-25 조사 · 확인된 것만)
- **Google Classroom 교사 홈(2026-07 개편)**: 모듈 순서 Recently Due → Class insights → **Feature spotlight** → Classes. 모듈 접기 가능·접은 상태 기억·내용 없으면 자동 숨김 — 원문 확인 https://support.google.com/edu/classroom/answer/17231999
- **NN/g 배너 블라인드니스**: 오른쪽 레일은 광고 자리로 읽혀 통째로 무시되기 쉬움. 예시 = 레일이 본문 25%인데 시선 0.8%(참가자 1명 사례 — 일반 수치로 인용 금지) — 원문 확인 https://www.nngroup.com/articles/banner-blindness-old-and-new-findings/
- (조사 에이전트 보고 · 원문 미열람) GitLab Pajamas feature discovery: New 배지는 페이지당 하나, 한 번 보면 영구 닫힘, 30~90일 자동 만료 https://design.gitlab.com/patterns/feature-discovery/ · NN/g: 새 기능 팁을 앱 실행 직후 띄우는 push 방식 비권장 https://www.nngroup.com/articles/onboarding-tutorials/ · Slack·Teams 소식 월 단위 묶음.
- 한국 서비스(클래스팅·하이클래스·웨일·뤼튼)의 인앱 안내 방식은 **확인 못 함** → 해결편 벤치마크에 쓰지 않는다.
- 목업 적용: `활동지로 바로 채점`의 New 배지는 페이지에 하나뿐. 만료 규칙(첫 사용 시 또는 60일 뒤 사라짐)은 PRD에 적을 것.

## 8. 남은 것
- 학교 설정 › 학생 화면의 동의서 링크 · 요금제 체크리스트 배너는 목업 없음(필요할 때).
- 답안 받기 전 버튼 = **`과제물 업로드`**(09-25 올립: '스캔 PDF 올리기'는 형식을 못박음). 도착 = 과제물 관리 반 화면(`?nav=list&c=1&t=A`)의 학급 일괄 업로드(PDF 분할 / 여러 파일).
- **카드엔 할 일 버튼 하나만**(09-25 올립): 답안 받기 전 카드의 '학생 제출로 받기' 링크를 뺐다. 받는 방법 바꾸기(학생 제출)는 **업로드 상세 화면(`task_ocr_v3` PDF 분할 업로드)에서 안내**할 것 — 아직 안 그림.
- New 배지 외 '첫 사용 한 줄'은 아직 안 그림.
- ~~흐름 목업의 홈 교체~~ → **09-25 완료**: `quick_start_v1_260925.html`의 `#home`을 리뉴얼 디자인으로 이식했다. 흐름 페르소나(김하나 · 2학기 0개 · 1학기 사회)는 그대로다.
  - 상태: `#home`(두 갈래) · `#home-resume`(임시 채점 = 가져오기 전) · `#home-sample`(예시만 = 답안 받기 전, 새 해시) · `#home-waiting`(2학기 사회 = 답안 받기 전)
  - 가져오기 완료 뒤 홈: 실제 = `점수 저장 전` 카드 / 예시 = `답안 받기 전` 카드
  - CSS는 `#s-home` 범위로 이식했다. **홈을 고치면 두 파일을 같이 고친다.**
- 해결편 덱 4장(첫 화면 목업)·7장(기존 사용자 영향)에 반영.

## 9. v2 확정 (09-26 올립) — 목업 `output/home_renewal_v2_260926.html`

**어떻게 여기까지 왔나(요약)**
1. **카드 안 할 일(v1)**: 평가 여럿 · 반 여럿 · 반별 공개 · 테스트 평가 · 학생 수 문제가 나올 때마다 규칙을 덧댔다. 올립: "카드에 늘리면 너무 많아 보이고, 바로 수업에 못 들어갈 것 같다". **VOC에 홈 개인화 요청은 없었다.**
2. **LMS 벤치마크**(Classroom Recently due는 원문 확인, Canvas To Do 등은 에이전트 확인): 할 일은 카드 **밖** 합친 목록에 두고, 카드는 입구다. '카드마다 할 일 + 버튼'은 확인한 서비스에 없다.
3. **DB(26-1 유료 2,491명)**: 수업 중앙값 2 · 84%가 4개 이하 · 반별 분할 62%. 올립 판단으로는 한 수업에 여러 반을 넣는 경우가 더 흔하다. 원본은 `../../../clipo_traffic_analysis/context.md` §3.999c다.
4. **목록으로 전환 → 덜어냄**: "Classroom처럼 많이 나오지 않게, 수업 카드가 주인공". 3열 타일 → 알약(필터처럼 보여 기각) → **2열 목록 상자**를 거쳤다.
5. **디자인**: 전부 흰 카드라 주목점이 없었다 → 역할별로 모양을 갈랐다(목록 · 진행 막대 카드 · 소식 목록). 제목 위계와 순서도 정리했다.

**확정 구조**
| 순서 | 영역 | 규칙 |
|---|---|---|
| 1 | **내 할 일**(22px) · 오른쪽 `더보기 ›` | 흰 상자 2열 목록. 임시 채점(N일 남음) → 확인 요청(요청자 · 날짜, 메모는 title) → 점수 저장 전 → 마감 전 → 채점 전. 이번 학기 + 확인 요청 + 임시 채점, 테스트 반 제외. 처음 4개. 점은 회색. **없으면 통째로 숨김**. 개발 홈 확인 요청 띠 · 카드 배지를 흡수 |
| 2 | **내 수업**(22px) 줄 | 정렬 · 보관한 수업 보기 · 활동지로 바로 채점(NEW) · 수업 만들기. 수업 목록 바로 위 |
| 3 | 학기 목록 | 모든 학기가 같은 한 줄. 이번 학기만 펼침. 학년도 조회 · '이전 학기' 묶음 없음(2025학년도 예시 데이터 포함) |
| 4 | 이번 학기 뒤 '지난 학기 이후 달라진 것' | 선으로 떼고 17px. 사용 이력 기준 노출(§6 아래 조건) |
| 레일 | 공지(연수 고정 D-N) · 캠페인 배너(연수 강사 안내 · 채점기준 꿀팁) · 자료실(기능 배우기 · 동의서) | §6 아래 레일 메모 그대로 |

**수업 카드 v2**
- 첫 글자 아이콘(같은 연한 파랑) · 수업명 · `반 · 학생 수`(말줄임 + title) · 평가 진행 막대 · `평가 N개 중 M개 마감`. 높이는 약 147px다.
- 막대 한 칸 = 평가 하나(모든 반 합산). 진한 파랑 = **마감**(09-26 올립 확인: 교사가 누르는 동작 · 평가 단위), 연한 파랑 = 진행 중, 회색 = 시작 전.

**행동 이름**
- 점수 저장 전 → `채점 점수 저장`
- 마감 전 → `마감`(→ 수행평가 채점 목록 `task_ocr_v3_260623.html#scoring`). 문구는 `마감하면 세특에 쓸 수 있어요`(이점 먼저)
- 확인 요청 → `검토` · 채점 전 → `AI 채점` · 임시 채점 → `수업에 옮기기`(줄 문구 `임시 채점 · 29일 남음` — '가져오기 전'은 뜻이 안 보여 09-26 교체)

**알리는 자리(3단)**: 홈 맨 위 큰 배너는 쓰지 않는다.
- 새 기능 = 홈 '달라진 것' → 기능 화면 얇은 배너 + `NEW`
- 이벤트 = 레일
- 모두에게 영향이 큰 공지 = 홈 맨 위 얇은 공지 띠
- 간편세특 배너(`quick_seteuk_dev_list_v1_260604.html`)는 방향이 맞다. 다듬을 점은 한 줄 높이로 줄이기 · 버튼을 공지 글이 아니라 바로 해 보는 곳으로 · 사라지는 규칙 추가다(아직 안 고침).

**검수(09-26)**
- 4상태 정상 · 콘솔 오류 0 · 빈 핸들러 0 · 옛 문구 0.
- 14px 미만은 `NEW` 태그(제품 12px)뿐이다.
- 쓰이지 않는 CSS 48줄을 정리했다.
- 흐름 목업 이식 때 `.fi` 클래스 충돌이 있었다 → 홈 쪽을 `.hfi`로 바꿨다.

**남은 것**
- 간편세특 배너 다듬기 · 알림 3단 규칙을 기능 화면에 적용
- 창체 · 행특 등 **학기 말 일**을 홈에서 알릴지(별도 질문)
- **업로드 상세 화면**: P1 올리는 벽 + 학생 제출 안내
- 해결편 덱 4장(첫 화면 = v2)과 7장(기존 사용자 영향)을 v2 기준으로 다시 쓰기
- 성공 지표
  - 내 할 일 항목 클릭 → 완료 전환(점수 저장 · 마감 · 검토)
  - '활동지로 바로 채점' 시작률(옛 투어 12%와 비교)
  - 고정된 연수 공지 클릭률
- P2 원인 인터뷰(저장 안 함)

**기존 사용자 영향(해결편 7장, v2 기준으로 갱신)**
- **그대로인 것**: 상단바 · 왼쪽 메뉴 · 카드를 누르면 수업 홈 · ⋮ 메뉴 · 학기 묶음 · 정렬 · 보관 토글 · 수업 만들기 팝업.
- **바뀌는 것**
  - 온보딩 배너 제거(빈 학기 두 갈래로 대체)
  - '내 할 일' 목록 추가(없으면 숨김) · 확인 요청 띠 → 목록 흡수
  - 수업 카드에 반 · 학생 수 · 진행 막대
  - 제목 줄에 활동지로 바로 채점
  - 지난 학기 접힘
  - 새 기능 소식
  - 레일 재구성

## 5. 재료
- 개발 확정 홈: `output/home_v1_260612.html`(카드·학기 섹션·레일·확인 요청 띠·온보딩 배너). 수업 만들기 팝업: `output/class_create_v1_260429.html`(흐름 목업에 `#cmModal`로 이식됨).
- 초안: `output/home_renewal_draft_260925.html` `#home` · `#home-waiting` · `#home-resume`.
- 블로그(검토용 주소): https://obkim-lgtm.github.io/clipo-blog/ — 배우기 `learn.html` · 과목별 채점 예시 `guide/examples.html` · 연수 `training.html`(쿠폰 문구 원본) · 소식 `news.html` · 영상 `video.html?v=R1`. 정식 공개 때 `blog.clipo.ai`.
- 연수 사실: 참가비 없음 · Zoom 90분 · 끝까지 참여 시 AI 크레딧 200개 쿠폰 14일(블로그 `pages/training.html`).
- 새 기능 후보: 채점 결과 공동 확인(09 마지막 주 오픈) · 학생 직접 제출. 'AI가 읽은 답안 확인·수정'은 10월 마지막 주 예정이라 아직 노출 금지(`service_overview.md`).
- 원칙: 까망이는 요청할 때만 · 팝업보다 인라인 · 같은 행동 두 군데 금지 · 안내는 행동하는 자리에서.
