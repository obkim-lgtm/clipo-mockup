# 과제물 인식·재채점 — 2026-09-03 작업 정리

> 다음 세션(fable 검수용) 이어받기 문서. 정책 원문은 PRD [C-2609 과제물 인식 확인 후 재채점](https://app.notion.com/p/38017e5c8cf180af9db1d78594db03bb).
> **이안이 이미 이 PRD를 보고 백엔드 작업 중** — 정책 전반 업데이트 판단은 올립과 함께.

## 1. 오늘 고친 것 (14커밋, 전부 배포됨)

### 연결·정합성
| 무엇 | 원인 |
|---|---|
| 드롭다운으로 옮기면 구버전 화면이 떴다 | 중고등 상세의 `BASE_FILE`이 옛 초등 루브릭(`260702`)이었음 |
| 초등 드롭다운 명단이 학급 현황과 달랐다 | 상태 4건·직접 입력 인원이 어긋나 있었음 |
| 까망이 `과제 입력 제출`(정현우)이 초등으로 감 | 정현우는 중고등 |
| `직접 채점 · 해제` 링크가 이름을 안 넘김 | 상세가 `김피드백`으로 떴음 |
| 점수 일괄 적용 돌아가기가 5월 파일로 | 폴백을 학급 채점 현황으로 |
| **실행 모달에서 미제출이 신예린으로** | 실제 미제출은 임도윤 — 서로 바뀌어 있었음 |
| 실행 모달 선생님 채점 상태가 표와 불일치 | 김서윤·한지우·노유찬(완료), 이준호(확인 필요) |

### 화면 깨짐
- **CSS 주석이 안 닫혀 10줄이 통째로 먹힘** (`/* … 툴팁(블랙,` 뒤 `340px) */` 유실).
  `.ec-summary-tip`이 position/opacity를 잃어 **문항 제목이 두 번 찍혔다.**
  6개 파일 `<style>` 주석 짝 전수 검사 완료 — 나머지 이상 없음.

### 판정 로직 — **같은 실수가 세 번 반복됨**
> 4상태로 정리하면서 상태 라벨은 합쳤는데 **판정은 옛 문자열을 그대로 봤다.**

| 학생 | 증상 | 고친 방식 |
|---|---|---|
| 김도현(10111) | 인식 성공인데 인식 결과가 안 보임 | `scoreFail + ocrDone` 분기 신설 |
| 임도윤(10109) | 미제출인데 남의 과제물 스캔이 보임 | `status==='미제출'` → `unsubmitted` 플래그로 판정 |
| 최유진(10104) | 채점 결과가 있는데 "아직 실행 안 함"으로 보임 | 채점된 학생 경로로 되돌림 |

### 문구 (11군데)
기준 셋 — ① 원인을 모르면 원인을 말하지 않는다 ② 주어를 제품이 아니라 교사가 마주한 상태로 ③ 화면 문구가 실제 케이스와 일치해야 한다.

- `네트워크 오류 등으로` 삭제 · `업데이트 전에 올린 과제물이에요` → `이 채점에는 인식 결과가 없어요`
- **파일 교체 배너가 「답안 인식 내용이 변경」이라 사실과 달랐다** → `과제물이 바뀌었어요`
- 토스트 `~습니다` → `~어요` 통일

### 연결 (오늘 뒤늦게 발견)
- **`점수 일괄 확정하기`가 토스트만 띄우고 끝났다** — 8/28 주석("그 화면은 별도")이 남아 있었고, 9/1에 그 화면을 만들고도 안 이어줬다. 이제 `?from=`으로 왕복
- **점수 일괄 적용 경고가 과제물 교체만 잡았다** → 인식 결과 수정도 포함, 사유는 이름 옆 칩으로

### 새 화면
- **직접 입력 제출 답안 화면** — `scoring_direct_write_v1`에서 이식. `문항 1` 알약 태그 없이 `문항 1. 발문` 한 줄(올립 지정). 발문·본문은 인식 결과 드로워에서 읽어 옴
- `학생 풀이와 답안 보기` → 인식 결과 탭 전용
- **직접 채점(AI 제외)** — 켜면 `저장 후 AI 채점` 잠금 + 우측 배너 + 드로워 재진입 시 복원

## 2. 케이스 ↔ 학생 (15명)

| PRD 케이스 | 학생 |
|---|---|
| 정상 | 조민재(10107) 교사 조정 · 한지우(10106) 직접 입력 |
| 인식 불확실 | **이준호(10102)** |
| 정확도 낮음 | **최유진(10104)** — 채점기준 전부 미도달, 18점(26-09-04 채점기준 3개·30점 만점으로 축소 후) |
| 인식 실패 | **백시우(10114)** |
| 채점만 실패 | **김도현(10111)** |
| 인식 수정 후 미재채점 | **박지민(10103)** |
| 파일 교체 | **송하림(10112)** |
| 레거시 | **노유찬(10113)** |
| 직접 채점(AI 제외) | **류지안(10116)** ← 오늘 추가 |
| ~~문항 변경~~ | **범위 밖** — 26-08-26 문항 트랙 이관. 오세진(10115)을 넣었다가 되돌림 |
| 미채점 / 채점 중 / 미제출 | 신예린(10110) / 윤하은(10108) / 임도윤(10109) |
| 초등 화면 | 김서윤(10101) |

## 3. 내일 판단할 것

0. **PRD 3-4 표의 `문항 변경` 행** — 이 PRD가 정의하지 않는데(문항 트랙 소관) 표에 남아 있어, 오늘 그것만 보고 목업에 케이스를 넣었다가 되돌렸다. 행을 뺄지 '범위 밖' 표시를 할지
1. ~~**직접 채점 지정 자리**~~ — **26-09-04 1안으로 확정·반영(올립).** 드로워 헤더 토글을 없애고 **우측 채점 패널 맨 위 고정 줄**(`selfGradeRow`, `이 학생 직접 채점 (AI 제외)` + 토글)로 이동. 채점 전·후 어느 상태에서든 같은 자리. 켜면 보라 줄 + `AI 채점 실행` CTA·[다시 채점]·재채점 배너 감춤 + 드로워 `저장 후 AI 채점` 잠김 + 빈 상태 문구가 "직접 채점하는 학생이에요"로. 미제출·채점 중은 줄 자체를 숨김. 상태는 여전히 `sessionStorage.clipo_ocr_exclude`(끈 기록이 명단 고정값보다 우선 — 학급 채점 현황·과제물 관리 실행 모달도 같은 규칙). PRD 학생별 케이스 표·QA 표의 "드로워" 문구 2곳 갱신함.
2. ~~**중고등 문항별 피드백 내용이 과제물과 불일치**~~ — **26-09-04 반영 완료.** `scoring_secondary_v1`·`scoring_secondary_current_v1` 두 파일의 문항별 5개(`문항 1`~`문항 5` 칩 + 발문, 점수는 채점요소별과 동일 10·8·10·10·8)·총평(`OVERALL`)·선생님 한마디 초안·루브릭 기반 AI(미제출 임도윤, 6요소)·채점 이력 더미·인식 결과 드로워 숨은 textarea까지 『사라진 스푼』으로 교체. 초등 「우리 고장」 문구 0건 확인
3. **`작성 과정 보기` 링크** — 직접 입력 화면에서 뺐음(연결할 흐름 없음). 작성 과정 기록 목업으로 이어줄지
4. **`AI Assistant의 채점 결과는…` 고지문** — 문체(`~습니다`)·영문 내부 명칭 노출. 개발 확정 화면이라 이번 범위 밖으로 뒀음
5. **설문 채점 화면**(`scoring_survey_v1_260623`) — 옛 더미 명단 + 폐기된 `재채점 필요` 상태. 창체 설문 쪽이라 범위 밖(올립 "안 맞춰도 돼")

## 4. 손대지 않기로 한 것
- **초등 목업** — 중고등 정의를 계승, 개발 시 어노테이션으로 설명(26-09-02 결정). 오늘은 툴바·명단·문구만 맞춤
- **임도윤 총점 `10 / 60`** — 미제출 점수는 평가 설계에서 입력 가능(올립 확인)

## 5. 오늘 배운 것 (지침에 반영함)

| 사고 | 반영한 곳 |
|---|---|
| 상태를 합쳤는데 판정은 옛 문자열 — 하루 3번 | **새 메모리** `feedback_status_merge_judgment` |
| CRLF 파일에 `
`으로 줄을 이어 붙여 나중에 3명이 한 줄로 날아감 | `feedback_bulk_edit_safety` |
| CSS 주석 `*/`를 먹어 10줄이 통째로 죽음 | `feedback_bulk_edit_safety` |
| 같은 명단이 5개 파일에 복제돼 사실이 어긋남 | `feedback_mockup_deploy_check` |
| 인라인 `style`을 클래스로 이기려다 파랑 위 파랑 글씨 | `feedback_mockup_deploy_check` |
| PRD 표의 행만 보고 범위 밖(문항 변경) 구현 | `feedback_prd_scope_level` |

## 5-1. 26-09-04 추가 변경
- **학생 채점 상세 3파일(`scoring_secondary_v1`·`scoring_secondary_current_v1`·`scoring_elementary_v3`)에서 까망이(목업 내비게이터)와 그 밑에 깔려 있던 `채점 플로우` 스텝 패널을 제거**(올립: 상세 화면에선 의미 없음 — 케이스 이동은 학생 드롭다운으로). 이어서 **학급 채점 현황(`class_scoring_detail_v3`)에서도 제거**(올립). 이제 까망이는 과제물 관리(`task_ocr_v3`)에만 남아 있고, 거기서 학급 채점 현황·상세로 들어오는 링크는 유지.
- 토스트·안내 `~습니다` 잔여 12종 → `~어요` 통일(중고등 2·초등·학급 채점 현황).
- **문구 판단 반영(올립 동의)**: ① 재채점 모달 환불 안내 → `이전 실행에 쓴 크레딧 1개는 돌려받았어요.`(원인 단정 삭제) + 노출 조건을 옛 이름(`채점실패`)에서 `scoreFail`·`ocrCase==='failed'` 플래그로 → 김도현·백시우 다시 채점 때 실제로 뜸 ② 내부 명칭 정리 — `과제물 AI`→`AI 채점`, `루브릭 (기반) AI`→`채점기준 기반 AI`, 미제출 빈 상태 제목 `제출된 과제물이 없어요`. 섹션 제목·도움말·모달 제목·공개/PDF/미리보기 옵션 라벨·이력 드로워 탭까지 3파일 일괄. ⚠️ **PRD 3-2엔 채점 실패 시 크레딧 반환 행이 없다** — 화면은 반환을 전제하니 이안 구현 기준이 되려면 PRD에 한 줄 필요(올립 판단).
- **PRD V1.1 (2026.09.04) 새로 씀 — 레나 QA용.** 올립이 V1.0 본문을 토글(`2026.08.31 - 최종 앞두고…`)로 접었고, 그 위에 V1.1을 넣음(insert_content, 댓글 5개 스레드 보존). 구성 = 문제 정의 / 무엇을 바꾸는가 9행 / 상태·정책 규칙(두 축·케이스 8종 표를 **인식 결과 영역·채점 영역 2열로 분리**·크레딧·직접 채점·재채점 유도) / **헷갈리기 쉬운 것 Q&A 8개** / QA 점검표 26행 / 비범위 / 논의 요청 2건 / 참고 시안·학생별 케이스 14명. 문구·화면 배치는 안 적음(디자인 단계). 위 어긋남(§5 정확도 낮음 줄·오세진 행)은 V1.1에서 해소. **교사 조정(조민재 AI 54→최종 50) 케이스는 올립 지시로 삭제** — 목업도 최종 54로 맞춤, 조민재 = 정상(파일) 기준 화면.
- **채점 실패 시 크레딧 반환 = 확정(올립, 26-09-04).** V1.1 크레딧 표 아래 규칙 한 줄 + Q&A·QA 24·25행에 반영, 논의 요청에서 제거. 화면의 환불 안내 문구와 일치.
- **정확도 낮음 판정 기준 = 불확실 문항 70% 이상(구 버전 값 유지, 올립 26-09-04).** V1.1 케이스 표 조건·QA 21행에 반영, 논의 요청 섹션 비움. 올립 재확인: 인식 실패인데 채점 성공은 없음(연쇄), 인식 성공·채점 실패는 있음 → Q&A 1행에 명시. **V1.1 미결 0건.**

- **채점기준 6개 → 3개(올립 26-09-04, 스크린샷 편의).** `내용 이해와 요약`(문항 1·2) / `생각의 변화와 진로 연계`(문항 3·4) / `후속 탐구 계획과 보고서 구성`(문항 5+형식). 총점 30, 정상 AI 26(8·10·8), 정확도 낮음 18, 류지안 22. 문항 5개는 그대로. 학급 채점 현황 점수·PRD 학생별 케이스 표 숫자도 같이 갱신. 초등 파일은 원래 3개라 그대로.

- **"AI 피드백 내용이 날아감" 오인 사고(26-09-04).** 보기 설정 키 `clipo_scoring_view_v1`을 현재 버전·인라인 수정 두 파일이 공유하는데, 현재 버전의 `채점 근거만 보기`가 `feedback:false`를 저장 → 인라인 수정 파일은 카드 안에 피드백만 있어 빈 카드로 보임. 고침: 인라인 수정 파일은 그 플래그로 **AI 피드백 섹션 전체(`#aiFeedbackSection`)를 접음**. 검수 중 카드가 비면 보기 설정 → `AI 피드백` 스위치부터 확인.

## 6. 검수할 때 유의
- **콘솔 에러 판정은 새 탭에서.** 같은 탭 이동은 이전 페이지 에러가 버퍼에 남는다
- **직접 채점 토글은 sessionStorage**(`clipo_ocr_exclude`)에 남는다. 한 번 켜면 다른 학생 화면에도 배너가 따라오니, 검수 전 `sessionStorage.removeItem('clipo_ocr_exclude')`
- 미리보기 3457 포트에 좀비 서버가 붙는 일이 잦다 — 404면 `netstat -ano | grep 3457` 로 중복 LISTENING 확인
