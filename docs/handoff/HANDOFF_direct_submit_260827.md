# 학생 직접 제출 개선 (C-2608) — 이어받기 (2026-08-27)

> 새 세션은 이 문서만 읽으면 이어서 작업할 수 있다.
> PRD는 **v0.4가 최종**이고 미결 0건. 지금 남은 일은 **목업 플로우 수정 1건**뿐이다.

## 지금 하던 일 (여기서 끊김)

**평가 설계 안의 문항 입력 플로우 점검.** 브라우저로 돌려보다 버그를 하나 찾은 상태에서 중단.

### 찾은 버그 — 아직 안 고침

`직접 작성`을 켜면 문항 1개가 생성되는데(`QUESTIONS.length === 1`), **설계 화면의 문항 요약 박스는 여전히 `작성한 문항 없음`으로 뜬다.** 교사 눈에는 켰는데 아무 일도 안 일어난 것처럼 보인다.

- 원인 : `updateSummary()`가 **내용이 채워진 문항만** 센다 (`filled.length === 0`이면 empty 상태)
- 위치 : `output/task_direct_write_v1_260729.html` → `updateSummary()`
- 재현 : `applyDesignState(0,{withQ:false,writeOn:false})` → `toggleWay('write')` → 요약 박스 확인

**제안하던 수정 방향** (확정 아님, 올립 컨펌 필요)

| 상태 | 요약 문구 | 버튼 |
|---|---|---|
| 문항 1개 · 내용 빔 | `문항 1개 · 내용 미입력` (경고 스타일 유지) | `문항 입력` |
| 내용 채움 | `문항 N개` | `문항 수정` |
| 제출 1명 이상 | `문항 N개 · 제출 N명` | `문항 보기` |

경고 스타일(빨강 점)을 빈 내용일 때 유지하는 이유 — 내용이 비면 **설계 저장이 막히므로** 경고가 맞다.

### 함께 볼 것

- 문항 입력이 지금은 `요약 박스 → 드로워` 2단계다. PRD는 "평가 설계 안에서 처리, 별도 페이지 신설 없음"까지만 정했고 드로워는 2026-08-07 회의 결정이라 유지가 맞다. 다만 **켜자마자 빈 문항 1개가 생겼다는 사실이 설계 화면에 보여야** 한다
- 드로워 진입 시점을 PRD는 "채점기준 입력 후"라고 적었는데 목업은 언제든 열린다 (목업 제약, 실제 구현에서 맞추면 됨)

## PRD

**[C-2608 학생 직접 제출 개선](https://app.notion.com/p/3ac17e5c8cf181acbd5be82063820274) — v0.4가 최종.** 미결 0건.

⚠️ **v0.1~v0.3은 지우지 말 것.** 코멘트 13건이 그 블록들에 앵커돼 있어서 `replace_content`로 갈아엎으면 날아간다. v0.3은 토글로 접지 않고 `⛔ 구버전` 표시만 달아 뒀다. 수정은 항상 `update_content` 부분 수정으로.

### 확정 정책 (v0.4 — 옛 값 쓰지 말 것)

- **문항 번호 입력란 없음.** 화면의 `문항 1`·`문항 2`는 순서 표시용 시스템 값. `1-1`, `가` 같은 표기는 교사가 **문항 내용 안에** 직접 씀
- **문항 내용 1글자 이상 필수.** 빈 문항 있으면 설계 저장 불가. 예외는 **이전된 기존 과제**뿐
- 직접 작성을 켜면 **문항 1개 자동 생성** → 문항 0개 상태·`준비 중` 학생 화면 없음
- 문항 1~20개, 추가 순서 고정(재정렬 없음), 문항 내용 최대 1,000자
- **권장 글자 수**(문항별) — 넘어도 입력·제출 가능, 학생 화면 글자 수만 빨강. 시스템 상한 5,000자
- **제출 후에도 문항 추가·수정·삭제 가능.** 삭제는 `제출 1명 이상`이면 확인 1회 (**임시 저장만 한 학생은 인원에 안 셈**)
- **삭제된 답안** = 학생 화면에 **내가 쓴 글이 그대로 보이고** 문항 옆 `삭제된 답안` 배지 + 입력 불가.
  제출했던 글이든 임시 저장이던 글이든 **같은 모양**(분기 없음). 교사 채점·결과 공개 화면엔 미표시
- **화면을 켜둔 사이 문항 구성이 바뀌면** 제출·임시 저장 클릭 시 안내 + 새로고침 유도 (삭제 정책과 **별개**로 필요한 처리)
- **제출 방식 해제 = 확인 후 허용.** 이미 낸 답안은 문항 삭제와 같은 규칙으로 보존.
  단 제출 1건 이상이면 **마지막 남은 방식은 못 끔**
- **결과 공개 후 재제출 = 불가.** 결과 공개되면 학생은 그 뒤로 결과 공개 화면만 봄
- **수동 마감이 `마감 후 제출 허용`을 이김**
- 채점 후 재제출 **별도 안내 안 함**(엣지케이스) · 답안 다운로드 **PDF 단일**
- 교사 채점 화면에 **문항별 작성 글자 수 표시** (이미 목업에 있음)
- **교사는 임시 저장을 전혀 못 봄** — `작성 중 · N자` 표시도 없음. 그냥 `미제출`
- 기존 과제 이전 = **문항 1개(내용 빈 상태)**, 학생 답안은 그 문항에 그대로

### 상태별 판정 (현행 동작, 이번에 안 바꿈)

다나 정리 → [과제 상태에 따른 학생 액션 정리](https://app.notion.com/p/3c817e5c8cf1801f9ea2d64bd82d8b03). 8케이스 표를 PRD에 옮겨 담았다.

## 목업

`clipo_mockup/output/`, Pages `obkim-lgtm/clipo-mockup`. **작업 트리 깨끗함**(최신 커밋 `57f1391`).

| 파일 | 상태 |
|---|---|
| `task_direct_write_v1_260729.html` | 교사 설계·문항 드로워. **위 버그 1건 빼고 v0.4 반영 완료** |
| `student_submit_v1_260729.html` | 학생 제출. v0.4 반영 완료 (결과 공개 후 재제출 불가까지) |
| `scoring_direct_write_v1_260729.html` | 교사 채점. 문항 단위 답안 + **문항별 글자 수 이미 있음** |
| `focus_log_v2_260806.html` | 과제물 관리. 임시 저장 미노출 원칙이 주석에 박혀 있음 |

### 안 만들기로 한 것

- **학생 결과 공개 화면(초등/중고등)** — 2026-08-27 올립 결정. 플로우가 달라진 게 아니고 디자이너가 잡을 영역이라 목업 불필요
- **다나 8케이스 화면** — 현행 동작이라 목업을 만들면 "이번에 바뀌는 것"으로 읽힘

## 작업 시 주의

- **미리보기 서버 좀비** — `preview_start`가 3457을 못 잡고 다른 포트로 밀린 뒤 그 포트도 응답 안 하는 일이 잦다. `Get-NetTCPConnection -LocalPort 3457 -State Listen`로 PID 찾아 `Stop-Process`하면 해결. 2026-08-27에 python 2개가 물고 있었다
- **파이썬 일괄 수정 스크립트는 실패하면 전체가 저장 안 된다** — `assert` 걸고 마지막에 한 번 쓰는 구조라, 중간에 하나 틀리면 앞 수정도 다 날아간다. 실패하면 무엇이 저장됐는지 grep으로 확인부터
- **Notion `update_content`는 old_str이 여러 곳에 걸리면 배치 전체가 거부된다.** 구버전 토글에 같은 문구가 있어서 자주 걸림 → 관점 열(`<td>교사</td>`)이나 QA 번호를 앞에 붙여 유일하게 만들 것

## 관련 문서

- 메모리 : `project_clipo_direct_write` (확정 정책 전체)
- 슬랙 공유 포맷 : `feedback_slack_share_format` (2026-08-26 올립 지시)
