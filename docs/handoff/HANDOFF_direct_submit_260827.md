# 학생 직접 제출 개선 (C-2608) — 이어받기 (최종 갱신 2026-08-28)

> 새 세션은 이 문서만 읽으면 이어서 작업할 수 있다.
> **목업은 배포까지 끝났다.** 남은 일은 **PRD 반영**(올립 컨펌 후 별도 요청 예정)과 아래 §6 미결뿐.

---

## 1. 지금 상태 한 줄

평가 설계 플로우를 **설계(허브) + 과제 화면 만들기(전환 화면)** 2단으로 재설계해 `task_direct_write_v1_260729.html`에 반영·배포 완료. PRD v0.4는 **드로워·"별도 페이지 신설 없음"을 전제로 쓰여 있어 이번 변경과 어긋난다** → 개정 필요(§6).

---

## 2. 이번 재설계 — 결정과 근거

올립과 채팅으로 정한 것. **근거까지 남기는 이유는 다시 뒤집지 않기 위해서다.**

### 2-1. 큰 구조

- **설계는 한 페이지 카드 스크롤. 스텝바(위저드) 없음.**
  교사에게 이건 단계가 아니라 한 과제의 여러 면이고, 서로 참조하며 오간다(올립).
  게다가 **인터뷰상 작성 순서가 사람마다 다르다** — 채점기준 먼저 쓰는 교사도, 문항 먼저 쓰는 교사도 있다.
  그래서 순서를 강제하지 않고 **완성 검사는 저장 시점에만** 건다.
- **카드 순서: ① 기본 정보 → ② 채점 설계(성취기준·채점기준·점수 산출) → ③ 과제물 수집 방법 → ④ AI 채점 지침(선택)**
  기준→과제 순은 평가 설계 정석이자 가이드·해외 서비스 관행(올립). 현행 개발 화면 순서와도 같아 재학습 비용 없음.
- **HIAI 평가 설계 시안과의 일관성은 의도적으로 맞추지 않았다**(올립: HIAI 미확정이니 신경 쓰지 말고 교사 니즈로만).

### 2-2. 화면을 가르는 기준 — 두 번 고쳐서 나온 답

- 1차: "학생이 보는 콘텐츠(안내·문항·기간) vs 교사만 보는 설정" → **어색하다는 피드백.**
- 원인: **제출 방식이 그 경계에 안 들어간다.** 교사 설정이지만 학생 화면의 답 입력 영역을 그대로 결정한다(직접 작성=에디터 / 파일 제출=업로드 칸). 파일 형식·개수·복붙 금지도 같다.
- **최종 기준**: 학생 직접 제출 아래 설정은 사실상 전부 "학생 화면을 만드는 일"이므로 **통째로 전환 화면으로** 보낸다.
  설계에는 **"어떻게 걷을 것인가"(수집 방법 3카드)까지만** 남긴다.

```
평가 설계 (허브 · 카드 스크롤)          과제 화면 만들기 (전환 화면)
① 기본 정보                             좌: 제출 방식 → 제출 기간 → 과제 안내 → 문항
② 채점 설계                             우: 학생 화면 미리보기 (sticky · 입력 즉시 반영)
③ 과제물 수집 방법 3카드                 하단: 취소 / 저장  (저장 단위는 설계와 한 몸)
   └ 선택에 따라 '다음 단계' 박스 1개
④ AI 채점 지침 (선택) — 10월, 이번 제외
```

### 2-3. 문항 드로워 → 전환 화면 승격

- 근거: 문항을 쓸 때 교사의 질문은 **"학생에게 어떻게 보일까"** 하나뿐이라 미리보기가 상시 옆에 있어야 하는데, 드로워 폭에서는 좌우 분할이 안 나온다. 2026-08-25에 드로워 안 접이식으로 되살렸던 미리보기를 오른쪽 고정 패널로 승격한 것.
- **문항은 직접 제출 전용이 아니다**(올립) — 출력→오프라인 손글씨→스캔(교사 일괄 업로드)도 가능. **내년 "문항 탭"(AI 문항 생성)으로 독립 예정**이라 화면은 제출 방식 중립으로 지었다.
  단 **올해 정책은 좁게 유지** — 진입은 직접 작성 켠 과제만, v0.4 게이트 그대로.

### 2-4. 수집 방법 선택 → '다음 단계' 박스 하나

같은 스위치의 후속인데 교사 쪽(준비 팁)은 섹션 안 회색 배너, 학생 쪽은 별도 흰 섹션이라 형태가 어긋나 있었다.
**자리와 그릇을 `.next-box` 하나로 통일**하고 무게 차이는 안에서만 낸다.

| 수집 방법 | 다음 단계 박스 | 버튼 |
|---|---|---|
| 선생님 직접 일괄 업로드 | 준비 팁(읽을거리) | outline · 팝업 |
| 학생 직접 입력 또는 제출 | 과제 화면(만들 것) | **primary · 페이지 이동** |
| 과제물 없이 바로 채점 | 없음 | — |

- **동작(팝업 vs 페이지)까지 같게 만들지 않는다** — `보기`와 `만들기`는 다른 일이라 같게 하면 화면이 거짓말을 한다.
- 진입 버튼 라벨은 **`과제 화면 만들기` / `과제 화면 수정` 둘로 고정**. `문항 입력`·`문항 보기`처럼 상태마다 바뀌면 다른 기능처럼 읽힌다(올립). 이동 신호로 `arrow-right`.
- 상태(문항 미입력)는 **파란 점이 아니라 아이콘 색**으로 표시(올립). 아이콘과 점을 둘 다 두면 신호가 둘이 된다.

### 2-5. 안내 문구 위계 — **다른 목업에도 적용할 룰**

두 번 뒤집혀 나온 결론. 상세는 `docs/design-system.md` "안내 문구 위계", 메모리 `feedback_notice_hierarchy`.

| 단 | 언제 | 모양 |
|---|---|---|
| 상시 설명 | 늘 있는 필드 설명 | `.sec-desc` 회색 14px |
| **조건부 안내** | 조건 맞을 때만 뜨고 막지 않음 | **`.note`** — 박스 없이 파란 `info` 아이콘 + 본문색 |
| 경고·잠금 | 되돌릴 수 없거나 동작 제한 | `.alert-info warn` 주황 박스 |

- 파란 인포 박스(`.alert-info` 기본형)는 **쓰지 않는다.**
- 조건부 안내를 회색 보조 문구로 두면 **상시 설명과 구별이 안 돼 눈이 걸러낸다** — 이게 두 번째 지적의 원인이었다.

### 2-6. 그 밖에 확정된 것

- **활동지 탭은 제품에 안 들어간다**(올립) — 목업 전용이라 폐기. 내년 "문항 탭"으로 대체.
  이 파일 탭 밴드에서 제거 완료. `HANDOFF_hiai_eval_design_port_260826.md` §2-2도 갱신(충돌 해소).
- **④ AI 채점 지침 (선택)** — 라벨 확정. "도움 자료"는 *AI가 만들어 준다*로 오독되는데 방향은 **교사→AI**(빈 활동지·모범답안·채점 가이드를 준다). 내용은 HIAI 것 재사용 가능. **10월 예정, 이번 목업 제외.**
- 파일 제출을 켜면 **허용 파일 형식은 PDF·이미지가 기본 체크**. 기타 파일(HWP 등)을 체크하면 조건부 안내.
- 저장 확인 팝업 — `계속 작성` 제거 후 **우측 상단 X**로, 버튼은 **`저장 안 하고 나가기` / `저장 후 나가기`**.
  ⚠️ CLIPO `버튼은 단어형·"~하기" 금지` 규칙의 **의도적 예외**다. 둘 다 화면을 떠나므로 `저장`만으로는 남는지 나가는지 알 수 없다. 되돌리려면 올립 확인 후.
- 섹션 제목 아래 여백 **16px**(`.sec-hd` 기본 6px은 설명문이 있을 때 값). 답답하다는 지적을 두 번 받고 올린 값이니 낮추지 말 것.

---

## 3. 파일에 실제로 바뀐 것

`output/task_direct_write_v1_260729.html`

- `scr-design` : 과제 안내·제출 기간·**제출 방식** 블록이 전부 빠지고, 수집 방법 카드 아래 **`.next-box` 다음 단계 박스 2종**(`#tip-bar` / `#qsum`)만 남음. 별도 `학생 과제 화면` 섹션 없음.
- `scr-questions` : 드로워(`.qdw*`) → **전환 화면**. `.tw-split` 좌 편집 / 우 `.tw-prev` 학생 화면 sticky 패널(`.sp-*`). 1200px 이하 세로 스택.
- 신설 JS : `syncTaskScreen()` · `renderStuPrev()` · `prevPeriodText()` · `toggleFmt()`/`syncFmtNote()` · `addPrevAttach()`.
- `updateSummary()` : 제출 방식 + 문항 수 + 제출 인원 요약, 보조 줄에 기간·안내. 경고(`warn`)는 **저장을 막는 상태에만**. 버튼 라벨 2종 고정, 크기 40px/14px(`btn-sm`으로 되돌리지 말 것).
- `applyDesignState()` : **수집 방법(`opt.method`)·허용 파일 형식까지 카드마다 리셋**한다.
- 삭제한 죽은 코드 : `.qdw*` · `.qprev*` · `.tip-bar*` · `.ds-tag` CSS, `toggleQPrev`, `#btnQBack`, 상시 문구 2종.
- 까망이(`mock_nav_dw.js`) : **교사/학생 2탭**으로 분리(`tab:'t'|'s'`), 라벨 `문항 작성`→`과제 화면 만들기`. 캐시 `?v=260828b`.

### 이번에 고친 버그 3건
1. `직접 작성` 켜면 문항 1개가 생기는데 요약이 `작성한 문항 없음`으로 남던 것 → 3상태 분기.
2. **`문항 미작성` 배지 카드가 샘플 문항 3개를 싣고 열리던 것** — `openDesign(0)`이 `withQ` 기본값 `true`에 걸림 → `openDesign(0, false)`.
3. **`선생님 업로드 평가` 카드가 학생 직접 제출로 열리던 것** — 수집 방법을 안 넘겨 앞 카드 값이 남음 → `opt.method` 추가.

---

## 4. PRD

**[C-2608 학생 직접 제출 개선](https://app.notion.com/p/3ac17e5c8cf181acbd5be82063820274) — v0.4가 최종.**

⚠️ **v0.1~v0.3은 지우지 말 것.** 코멘트 13건이 그 블록들에 앵커돼 있어 `replace_content`로 갈아엎으면 날아간다. 수정은 항상 `update_content` 부분 수정으로.

### 확정 정책 (v0.4 — 옛 값 쓰지 말 것)

- **문항 번호 입력란 없음.** 화면의 `문항 1`·`문항 2`는 순서 표시용 시스템 값. `1-1`, `가` 같은 표기는 교사가 **문항 내용 안에** 직접 씀
- **문항 내용 1글자 이상 필수.** 빈 문항 있으면 설계 저장 불가. 예외는 **이전된 기존 과제**뿐
- 직접 작성을 켜면 **문항 1개 자동 생성** → 문항 0개 상태·`준비 중` 학생 화면 없음
- 문항 1~20개, 추가 순서 고정(재정렬 없음), 문항 내용 최대 1,000자
- **권장 글자 수**(문항별) — 넘어도 입력·제출 가능, 학생 화면 글자 수만 빨강. 시스템 상한 5,000자
- **제출 후에도 문항 추가·수정·삭제 가능.** 삭제는 `제출 1명 이상`이면 확인 1회 (**임시 저장만 한 학생은 인원에 안 셈**)
- **삭제된 답안** = 학생 화면에 **내가 쓴 글이 그대로 보이고** 문항 옆 `삭제된 답안` 배지 + 입력 불가.
  제출했던 글이든 임시 저장이던 글이든 **같은 모양**(분기 없음). 교사 채점·결과 공개 화면엔 미표시
- **화면을 켜둔 사이 문항 구성이 바뀌면** 제출·임시 저장 클릭 시 안내 + 새로고침 유도 (삭제 정책과 **별개**)
- **제출 방식 해제 = 확인 후 허용.** 이미 낸 답안은 문항 삭제와 같은 규칙으로 보존.
  단 제출 1건 이상이면 **마지막 남은 방식은 못 끔**
- **결과 공개 후 재제출 = 불가.** 결과 공개되면 학생은 그 뒤로 결과 공개 화면만 봄
- **수동 마감이 `마감 후 제출 허용`을 이김**
- 채점 후 재제출 **별도 안내 안 함**(엣지케이스) · 답안 다운로드 **PDF 단일**
- 교사 채점 화면에 **문항별 작성 글자 수 표시**
- **교사는 임시 저장을 전혀 못 봄** — `작성 중 · N자` 표시도 없음. 그냥 `미제출`
- 기존 과제 이전 = **문항 1개(내용 빈 상태)**, 학생 답안은 그 문항에 그대로

### 상태별 판정 (현행 동작, 이번에 안 바꿈)

다나 정리 → [과제 상태에 따른 학생 액션 정리](https://app.notion.com/p/3c817e5c8cf1801f9ea2d64bd82d8b03). 8케이스 표를 PRD에 옮겨 담았다.

---

## 5. 목업 파일

`clipo_mockup/output/`, Pages `obkim-lgtm/clipo-mockup`

| 파일 | 상태 |
|---|---|
| `task_direct_write_v1_260729.html` | 교사 설계 + **과제 화면 만들기**. 이번 재설계 전부 반영 |
| `student_submit_v1_260729.html` | 학생 제출. v0.4 반영 완료 (결과 공개 후 재제출 불가까지) |
| `scoring_direct_write_v1_260729.html` | 교사 채점. 문항 단위 답안 + 문항별 글자 수 |
| `focus_log_v3_260819.html` | 과제물 관리. 임시 저장 미노출 원칙이 주석에 박혀 있음 |
| `mock_nav_dw.js` | 까망이 공용(5종 파일 공유) — **여기만 고치면 전부 반영** |

### 안 만들기로 한 것
- **학생 결과 공개 화면(초등/중고등)** — 2026-08-27 올립. 플로우가 달라진 게 아니고 디자이너 영역
- **다나 8케이스 화면** — 현행 동작이라 목업을 만들면 "이번에 바뀌는 것"으로 읽힘
- **④ AI 채점 지침 화면** — 10월

---

## 6. 미결 (다음 세션 할 일)

1. **PRD 개정** — v0.4는 "평가 설계 안에서 처리, 별도 페이지 신설 없음" + 드로워 전제라 **이번 재설계와 어긋난다.** 올립이 컨펌 후 요청 예정(2026-08-28 기준 대기).
2. **활동지 탭 제거 전수 반영** — 이 파일만 뗐다. 다른 목업의 탭 밴드엔 아직 `활동지`가 있다.
3. **안내 문구 위계(`.note`) 다른 목업 확산** — 룰만 등록했고 적용은 이 파일뿐.
4. `저장 후 나가기` 라벨이 단어형 규칙 예외로 남아 있음 — 유지/환원 확인.
5. ④ AI 채점 지침(10월) · 실시간 모니터링(후속 배포).

---

## 7. 작업 시 주의

- **미리보기 서버 좀비** — `preview_start`가 3457을 못 잡고 다른 포트로 밀린 뒤 그 포트도 응답 안 하는 일이 잦다.
  `Get-NetTCPConnection -LocalPort 3457 -State Listen`로 PID 찾아 `Stop-Process`. 2026-08-28에 3개가 물려 있었다.
- **브라우저 캐시가 세다** — HTML 자체가 캐시돼 수정이 안 보인다. `?cb=n` 쿼리를 붙여 확인할 것.
- **공용 JS(`mock_nav_dw.js`)를 고치면 `?v=`를 올린다** — 6개 파일 전부.
- **파이썬 일괄 수정 스크립트는 실패하면 전체가 저장 안 된다** — 실패 시 무엇이 저장됐는지 grep으로 확인부터.
- 구글드라이브 위 git이라 커밋 후 수 분 멈출 수 있다(`gc.auto 0`로 완화). **타임아웃=실패 아님, `git log`로 확인.**

## 관련 문서
- 메모리 : `project_clipo_direct_write`(확정 정책) · `feedback_notice_hierarchy`(안내 위계) · `project_clipo_question_tab`(활동지 폐기)
- 디자인 룰 : `docs/design-system.md` "안내 문구 위계" · `docs/mockup-navigator.md` §2(까망이 탭)
- HIAI 포팅 : `docs/handoff/HANDOFF_hiai_eval_design_port_260826.md`
